---
import BaseLayout from '../../layouts/BaseLayout.astro';
import CLIPArchitectureDiagram from '../../components/CLIPArchitectureDiagram.astro';
import CLIPContrastiveLossDiagram from '../../components/CLIPContrastiveLossDiagram.astro';
import CLIPZeroShotDiagram from '../../components/CLIPZeroShotDiagram.astro';
import { todayYmd } from '../../utils/date';

const createdAt = todayYmd();

export const meta = {
    number: '19',
    title: 'CLIP：连接语言与视觉',
    description: '精读 OpenAI CLIP 论文 —— 双编码器架构、对比预训练策略与零样本迁移原理',
    tags: ["CLIP", "Multimodal", "Contrastive"],
    readTime: '22 min'
};
---

<BaseLayout title="CLIP：连接语言与视觉" description="精读 OpenAI CLIP 论文 —— 双编码器架构、对比预训练、零样本迁移">
    <header class="text-center mb-12">
        <h1 class="text-4xl font-bold mb-4 gradient-text">CLIP：用自然语言监督学习视觉</h1>
        <p class="text-xl text-gray-500">精读 OpenAI 2021 年 CLIP 论文 —— 双编码器、对比预训练与零样本迁移</p>
        <p class="mt-4 text-sm uppercase tracking-[0.2em] text-[color:var(--muted)]">添加时间 {createdAt}</p>
    </header>

    <!-- 前言 -->
    <div class="card">
        <h2>前言：为什么 CLIP 重要？</h2>
        <p>传统视觉模型训练需要大量人工标注数据，且只能识别预定义类别。2021 年 OpenAI 发表的 <strong>CLIP (Contrastive Language–Image Pre-training)</strong> 彻底改变了这一范式：</p>
        <ul class="list-disc pl-6 mb-4 space-y-2 text-gray-600">
            <li><strong>用自然语言做监督</strong> —— 不需要精心设计的标注体系，互联网上的图文配对就是训练数据</li>
            <li><strong>零样本迁移</strong> —— 不微调就能做分类、检索等任务，效果媲美专门训练的模型</li>
            <li><strong>开放词汇</strong> —— 类别不再是固定的 1000 类，而是任意自然语言描述</li>
        </ul>
        <div class="bg-blue-50 p-4 rounded-lg border border-blue-100">
            <strong>论文：</strong> <em>"Learning Transferable Visual Models From Natural Language Supervision"</em>，Radford et al., 2021。训练数据：从互联网收集的 <strong>4 亿</strong>图文对（WebImageText, WIT）。
        </div>
    </div>

    <!-- 第一章：架构 -->
    <div class="card">
        <h2>1. 双编码器架构</h2>
        <p>CLIP 的核心思想极其简洁：<span class="highlight">用两个独立的编码器，分别把图像和文本映射到同一个嵌入空间</span>，然后通过对比学习让匹配的图文对靠近、不匹配的远离。</p>

        <CLIPArchitectureDiagram />

        <h3 class="text-blue-600">图像编码器 (Image Encoder)</h3>
        <p>CLIP 论文实验了两种图像编码器：</p>
        <div class="overflow-x-auto">
            <table class="min-w-full divide-y divide-gray-200">
                <thead class="bg-gray-50">
                    <tr>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">架构</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">变体</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">输出</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">说明</th>
                    </tr>
                </thead>
                <tbody class="bg-white divide-y divide-gray-200 text-sm">
                    <tr>
                        <td class="px-6 py-4 font-medium text-blue-600">ResNet</td>
                        <td class="px-6 py-4 text-gray-600">RN50, RN101, RN50x4/16/64</td>
                        <td class="px-6 py-4 text-gray-600">全局平均池化后的特征向量</td>
                        <td class="px-6 py-4 text-gray-600">使用 attention pooling 替代 GAP</td>
                    </tr>
                    <tr>
                        <td class="px-6 py-4 font-medium text-blue-600">ViT</td>
                        <td class="px-6 py-4 text-gray-600">ViT-B/32, ViT-B/16, ViT-L/14</td>
                        <td class="px-6 py-4 text-gray-600">[CLS] token 的输出向量</td>
                        <td class="px-6 py-4 text-gray-600">最终 ViT-L/14@336px 效果最好</td>
                    </tr>
                </tbody>
            </table>
        </div>
        <p>论文发现 <strong>ViT 的计算效率更高</strong>：在相同计算预算下，ViT-L/14 比 ResNet 系列表现更优。这也推动了后来 ViT 在视觉领域的广泛采用。</p>

        <h3 class="text-blue-600">文本编码器 (Text Encoder)</h3>
        <p>文本编码器是一个标准的 <strong>Transformer</strong>（类似 GPT-2）：</p>
        <ul class="list-disc pl-6 mb-4 space-y-2 text-gray-600">
            <li>12 层 Transformer，512 维隐藏层，8 个注意力头</li>
            <li>最大序列长度 76 tokens（BPE 分词，词表 49152）</li>
            <li>取 <strong>[EOS] token</strong>（最高序列位置）的输出作为文本表示</li>
            <li>输出通过线性投影映射到共享嵌入空间</li>
        </ul>

        <h3 class="text-blue-600">关键设计：投影到共享空间</h3>
        <p>两个编码器各自输出的特征维度不同。CLIP 通过一个<strong>可学习的线性投影层</strong>，将图像特征和文本特征分别映射到同一个 <span set:html={"$d$"}></span> 维空间（论文中 <span set:html={"$d = 512$"}></span> 或 <span set:html={"$768$"}></span>），然后在这个空间里计算余弦相似度。</p>
    </div>

    <!-- 第二章：对比预训练 -->
    <div class="card">
        <h2>2. 对比预训练策略</h2>
        <p>CLIP 的训练目标是让模型学会"哪张图配哪段文字"。具体方法是 <span class="highlight">InfoNCE 对比损失</span>。</p>

        <h3 class="text-blue-600">核心流程</h3>
        <ol class="list-decimal pl-6 mb-4 space-y-2 text-gray-600">
            <li>一个 batch 有 <span set:html={"$N$"}></span> 个图文对 <span set:html={"$(I_1, T_1), (I_2, T_2), \\ldots, (I_N, T_N)$"}></span></li>
            <li>通过图像编码器和文本编码器分别得到嵌入向量</li>
            <li>计算 <span set:html={"$N \\times N$"}></span> 的余弦相似度矩阵（乘以可学习的温度参数 <span set:html={"$\\tau$"}></span>）</li>
            <li>对角线上的 <span set:html={"$N$"}></span> 个位置是正样本对，其余 <span set:html={"$N^2 - N$"}></span> 个是负样本对</li>
            <li>分别沿行和列做 softmax cross-entropy，取平均</li>
        </ol>

        <CLIPContrastiveLossDiagram />

        <h3 class="text-blue-600">数学公式</h3>
        <p>给定一个 batch 中的第 <span set:html={"$i$"}></span> 个样本：</p>

        <div class="math-block">
            <p set:html={"$$\\ell_{i}^{\\text{image}} = -\\log \\frac{\\exp(\\text{sim}(I_i, T_i) / \\tau)}{\\sum_{j=1}^{N} \\exp(\\text{sim}(I_i, T_j) / \\tau)}$$"}></p>
        </div>

        <div class="math-block">
            <p set:html={"$$\\ell_{i}^{\\text{text}} = -\\log \\frac{\\exp(\\text{sim}(I_i, T_i) / \\tau)}{\\sum_{j=1}^{N} \\exp(\\text{sim}(I_j, T_i) / \\tau)}$$"}></p>
        </div>

        <p>最终损失：</p>
        <div class="math-block">
            <p set:html={"$$\\mathcal{L} = \\frac{1}{2N} \\sum_{i=1}^{N} \\left( \\ell_{i}^{\\text{image}} + \\ell_{i}^{\\text{text}} \\right)$$"}></p>
        </div>

        <p>其中 <span set:html={"$\\text{sim}(I, T) = \\frac{I \\cdot T}{\\|I\\| \\|T\\|}$"}></span> 是余弦相似度，<span set:html={"$\\tau$"}></span> 是可学习的温度参数（初始值 0.07）。</p>

        <div class="note-box">
            <strong>为什么是对比学习而不是生成式？</strong> 论文对比了三种训练目标：(1) 图像预测文本（生成式），(2) 图像-文本匹配（bag-of-words），(3) 对比学习。对比学习的效率高出 <strong>4 倍</strong> —— 因为它只需要判断"匹配/不匹配"，而不需要逐词生成文本。
        </div>

        <h3 class="text-blue-600">训练规模</h3>
        <div class="overflow-x-auto">
            <table class="min-w-full divide-y divide-gray-200">
                <thead class="bg-gray-50">
                    <tr>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">参数</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">值</th>
                    </tr>
                </thead>
                <tbody class="bg-white divide-y divide-gray-200 text-sm">
                    <tr>
                        <td class="px-6 py-4 font-medium text-gray-700">训练数据</td>
                        <td class="px-6 py-4 text-gray-600">WIT（WebImageText），4 亿图文对</td>
                    </tr>
                    <tr>
                        <td class="px-6 py-4 font-medium text-gray-700">Batch Size</td>
                        <td class="px-6 py-4 text-gray-600">32,768（巨大 batch 是对比学习的关键）</td>
                    </tr>
                    <tr>
                        <td class="px-6 py-4 font-medium text-gray-700">训练轮数</td>
                        <td class="px-6 py-4 text-gray-600">32 epochs</td>
                    </tr>
                    <tr>
                        <td class="px-6 py-4 font-medium text-gray-700">优化器</td>
                        <td class="px-6 py-4 text-gray-600">Adam（解耦权重衰减），学习率 5e-4，cosine schedule</td>
                    </tr>
                    <tr>
                        <td class="px-6 py-4 font-medium text-gray-700">计算资源</td>
                        <td class="px-6 py-4 text-gray-600">256 张 V100 GPU，训练约 12 天（ViT-L/14）</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>

    <!-- 第三章：零样本迁移 -->
    <div class="card">
        <h2>3. 零样本迁移原理</h2>
        <p>CLIP 最令人惊叹的能力：<span class="highlight">不需要任何微调，就能在全新任务上做分类</span>。秘密在于用自然语言构造"分类器"。</p>

        <CLIPZeroShotDiagram />

        <h3 class="text-blue-600">推理步骤</h3>
        <ol class="list-decimal pl-6 mb-4 space-y-2 text-gray-600">
            <li><strong>构造文本 Prompt</strong>：对每个候选类别，用模板 <code set:html={'"a photo of a &#123;class&#125;"'}></code> 生成描述文本</li>
            <li><strong>编码所有文本</strong>：通过文本编码器得到每个类别的嵌入向量</li>
            <li><strong>编码测试图像</strong>：通过图像编码器得到图像嵌入</li>
            <li><strong>计算相似度</strong>：图像嵌入与所有文本嵌入计算余弦相似度</li>
            <li><strong>选择最高分</strong>：相似度最高的类别就是预测结果</li>
        </ol>

        <div class="note-box">
            <strong>Prompt Engineering 很重要！</strong> 论文发现 <code set:html={'"a photo of a &#123;class&#125;"'}></code> 比单独使用类名效果好得多。对于特定领域（如卫星图像），用 <code set:html={'"a satellite photo of a &#123;class&#125;"'}></code> 能进一步提升。论文甚至使用了 <strong>80 种 prompt 模板的集成</strong>（ensemble），在 ImageNet 上提升了 3.5%。
        </div>

        <h3 class="text-blue-600">零样本性能</h3>
        <p>CLIP 在 <strong>27 个数据集</strong>上评估了零样本性能，亮点包括：</p>
        <ul class="list-disc pl-6 mb-4 space-y-2 text-gray-600">
            <li><strong>ImageNet</strong>：零样本 76.2% top-1（匹配 ResNet-50 的有监督精度！）</li>
            <li><strong>STL-10</strong>：零样本 99.3%（比有监督方法还高）</li>
            <li>在 16 个数据集上，零样本 CLIP 超过了有监督的 linear probe ResNet-50</li>
            <li>在细粒度分类（如花、车型）和复杂任务（卫星图、计数）上仍有差距</li>
        </ul>
    </div>

    <!-- 第四章：关键应用 -->
    <div class="card">
        <h2>4. 关键应用场景</h2>

        <h3 class="text-blue-600">图像分类</h3>
        <p>构造类别 prompt，计算相似度即可。支持任意开放词汇分类，无需标注数据。</p>

        <h3 class="text-blue-600">图像-文本检索</h3>
        <p>给定文本查询，在图像库中找到最匹配的图像（text→image）；或给定图像，找到最匹配的文本描述（image→text）。核心操作都是在共享嵌入空间中做最近邻搜索。</p>

        <h3 class="text-blue-600">文本引导的图像生成</h3>
        <p>CLIP 的文本-图像对齐能力被广泛用于引导图像生成模型：</p>
        <ul class="list-disc pl-6 mb-4 space-y-2 text-gray-600">
            <li><strong>DALL·E 2</strong>：使用 CLIP 文本编码器作为条件输入</li>
            <li><strong>Stable Diffusion</strong>：早期版本使用 CLIP 文本编码器</li>
            <li><strong>StyleCLIP</strong>：用 CLIP 损失引导 StyleGAN 编辑方向</li>
        </ul>

        <h3 class="text-blue-600">多模态基础模型</h3>
        <p>CLIP 的预训练视觉编码器被许多 VLM 直接复用：</p>
        <ul class="list-disc pl-6 mb-4 space-y-2 text-gray-600">
            <li><strong>LLaVA</strong>：CLIP ViT-L/14 作为视觉编码器，接 LLaMA 语言模型</li>
            <li><strong>GPT-4V</strong>：据推测使用了类 CLIP 的视觉编码器</li>
            <li><strong>Qwen-VL</strong>：基于 CLIP 思路的视觉编码器</li>
        </ul>
    </div>

    <!-- 第五章：伪代码实现 -->
    <div class="card">
        <h2>5. PyTorch 伪代码</h2>
        <p>论文中给出了极简的 NumPy 风格伪代码，以下是等价的 PyTorch 版本：</p>

        <pre class="language-python"><code>{`# CLIP 前向传播与对比损失（伪代码）
import torch
import torch.nn.functional as F

# 编码器输出
I_e = image_encoder(images)        # [N, d_i]
T_e = text_encoder(texts)          # [N, d_t]

# 线性投影到共享嵌入空间
I_e = I_e @ W_i                    # [N, d]
T_e = T_e @ W_t                    # [N, d]

# L2 归一化
I_e = F.normalize(I_e, dim=-1)
T_e = F.normalize(T_e, dim=-1)

# 余弦相似度矩阵（缩放温度）
logits = (I_e @ T_e.T) * tau.exp() # [N, N]

# 对称交叉熵损失
labels = torch.arange(N)           # [0, 1, 2, ..., N-1]
loss_i = F.cross_entropy(logits, labels)     # 按行
loss_t = F.cross_entropy(logits.T, labels)   # 按列
loss = (loss_i + loss_t) / 2`}</code></pre>

        <p>这段代码完美体现了 CLIP 的优雅：<strong>不到 15 行</strong>就实现了核心训练逻辑。</p>
    </div>

    <!-- 第六章：CLIP 后续变体 -->
    <div class="card">
        <h2>6. CLIP 后续改进模型</h2>
        <p>CLIP 开创了"语言监督视觉学习"的范式，之后涌现了大量改进工作：</p>

        <div class="overflow-x-auto">
            <table class="min-w-full divide-y divide-gray-200">
                <thead class="bg-gray-50">
                    <tr>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">模型</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">团队</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">核心改进</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">优缺点</th>
                    </tr>
                </thead>
                <tbody class="bg-white divide-y divide-gray-200 text-sm">
                    <tr>
                        <td class="px-6 py-4 font-medium text-blue-600">ALIGN</td>
                        <td class="px-6 py-4 text-gray-600">Google, 2021</td>
                        <td class="px-6 py-4 text-gray-600">使用 18 亿噪声图文对，不做数据清洗，靠规模取胜</td>
                        <td class="px-6 py-4 text-gray-600">✅ 证明了数据规模可以弥补噪声<br/>❌ 数据未公开</td>
                    </tr>
                    <tr>
                        <td class="px-6 py-4 font-medium text-blue-600">FLAVA</td>
                        <td class="px-6 py-4 text-gray-600">Meta, 2022</td>
                        <td class="px-6 py-4 text-gray-600">统一架构：图像、文本、多模态三路编码器 + MLM/ITM/对比联合训练</td>
                        <td class="px-6 py-4 text-gray-600">✅ 单模态和多模态任务都强<br/>❌ 训练成本更高</td>
                    </tr>
                    <tr>
                        <td class="px-6 py-4 font-medium text-blue-600">OpenCLIP</td>
                        <td class="px-6 py-4 text-gray-600">LAION, 2022</td>
                        <td class="px-6 py-4 text-gray-600">开源复现 CLIP，使用 LAION-2B/5B 公开数据集训练</td>
                        <td class="px-6 py-4 text-gray-600">✅ 完全开源，社区可复现<br/>✅ 数据集公开</td>
                    </tr>
                    <tr>
                        <td class="px-6 py-4 font-medium text-blue-600">SigLIP</td>
                        <td class="px-6 py-4 text-gray-600">Google, 2023</td>
                        <td class="px-6 py-4 text-gray-600">用 sigmoid 替代 softmax，消除对全局归一化（即超大 batch）的依赖</td>
                        <td class="px-6 py-4 text-gray-600">✅ 小 batch 也能训好<br/>✅ 效果不输 softmax CLIP</td>
                    </tr>
                    <tr>
                        <td class="px-6 py-4 font-medium text-blue-600">EVA-CLIP</td>
                        <td class="px-6 py-4 text-gray-600">BAAI, 2023</td>
                        <td class="px-6 py-4 text-gray-600">用 EVA（masked image modeling）预训练的 ViT 初始化 CLIP 图像编码器</td>
                        <td class="px-6 py-4 text-gray-600">✅ 更强的视觉表示<br/>✅ 18B 参数，ImageNet 零样本 82%</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>

    <!-- 第七章：局限与思考 -->
    <div class="card">
        <h2>7. 局限性与思考</h2>
        <p>CLIP 虽然强大，但论文也坦诚地讨论了其局限：</p>

        <h3 class="text-blue-600">能力边界</h3>
        <ul class="list-disc pl-6 mb-4 space-y-2 text-gray-600">
            <li><strong>细粒度任务偏弱</strong>：区分花的品种、车的年款等需要专家知识的任务，零样本效果有限</li>
            <li><strong>抽象推理不足</strong>：计数、空间关系（"左边的 vs 右边的"）等任务表现不佳</li>
            <li><strong>分布外泛化有限</strong>：在 MNIST 这种与预训练分布差异大的数据集上，零样本仅 88%（低于预期）</li>
            <li><strong>社会偏见</strong>：训练数据来自互联网，不可避免地包含各种偏见</li>
        </ul>

        <h3 class="text-blue-600">为什么 CLIP 改变了游戏规则？</h3>
        <div class="quote-block">
            <p>CLIP 的意义不仅在于一个模型，而在于<strong>一种范式</strong>：用海量弱监督数据（自然语言）学习通用视觉表示，通过 prompt 灵活迁移到下游任务。这种"预训练 + prompt"的思路直接影响了后来的 GPT-4V、LLaVA、DALL·E 等里程碑模型。</p>
        </div>
    </div>

    <!-- 总结 -->
    <div class="card">
        <h2>总结</h2>
        <div class="overflow-x-auto">
            <table class="min-w-full divide-y divide-gray-200">
                <thead class="bg-gray-50">
                    <tr>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">维度</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">要点</th>
                    </tr>
                </thead>
                <tbody class="bg-white divide-y divide-gray-200 text-sm">
                    <tr>
                        <td class="px-6 py-4 font-medium text-blue-600">架构</td>
                        <td class="px-6 py-4 text-gray-600">双编码器（ViT/ResNet + Transformer）+ 线性投影到共享嵌入空间</td>
                    </tr>
                    <tr>
                        <td class="px-6 py-4 font-medium text-blue-600">训练</td>
                        <td class="px-6 py-4 text-gray-600">4 亿图文对，对比学习（InfoNCE），batch size 32K</td>
                    </tr>
                    <tr>
                        <td class="px-6 py-4 font-medium text-blue-600">推理</td>
                        <td class="px-6 py-4 text-gray-600">零样本迁移：文本 prompt 定义类别 → 余弦相似度 → argmax</td>
                    </tr>
                    <tr>
                        <td class="px-6 py-4 font-medium text-blue-600">核心贡献</td>
                        <td class="px-6 py-4 text-gray-600">用自然语言监督学习通用视觉表示，开创 "预训练 + prompt" 范式</td>
                    </tr>
                    <tr>
                        <td class="px-6 py-4 font-medium text-blue-600">影响</td>
                        <td class="px-6 py-4 text-gray-600">DALL·E 2、Stable Diffusion、LLaVA 等模型的基础组件</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>

    <div class="mt-8 text-center text-gray-500">
        <p>CLIP 证明了：当数据规模足够大、监督信号足够自然，视觉模型也能拥有语言的灵活性。</p>
    </div>
</BaseLayout>
