---
import BaseLayout from '../../layouts/BaseLayout.astro';

export const meta = {
    number: '26',
    title: 'Code as Agent Harness：代码如何成为 Agent 的运行基座',
    description: '深入理解 Code as Agent Harness：从推理、行动、环境到记忆、验证、多智能体协作与开放问题。',
    tags: ['Agent', 'Code', 'Systems', 'Multi-Agent'],
    readTime: '36 min',
    createdAt: '2026-05-20'
};
---

<BaseLayout title="Code as Agent Harness：代码如何成为 Agent 的运行基座" description="把代码从“输出结果”重新理解为“可执行、可验证、有状态的 agent harness”，并系统梳理其接口层、机制层、多智能体扩展与开放问题。">
    <header class="text-center mb-12">
        <p class="text-xs uppercase tracking-[0.3em] text-[color:var(--muted)]">Agent Systems / Code / Systems</p>
        <h1 class="text-4xl font-bold mb-4 gradient-text">Code as Agent Harness：代码如何成为 Agent 的运行基座</h1>
        <p class="text-xl text-gray-500">从“让模型写代码”到“让代码承载智能体运行”：一篇关于 agent 系统基础设施的综述</p>
        <p class="mt-4 text-sm uppercase tracking-[0.2em] text-[color:var(--muted)]">添加时间 {meta.createdAt}</p>
    </header>

    <div class="card">
        <h2>1. Introduction：这篇论文真正想改写的是什么问题？</h2>
        <p>
            过去几年里，大家谈到“代码 + 大模型”时，最自然的理解往往是：模型会写 Python、会修 bug、会提交补丁、会在 SWE-bench 上做题。
            这种理解当然没错，但它默认了一件事——<span class="highlight">代码只是模型生成出来的产物</span>。
            论文 <em>Code as Agent Harness</em> 想做的，恰恰是把这个默认前提翻过来：在很多 agent 场景里，代码不是“最后留下来的文本结果”，而是智能体用来组织推理、执行动作、表示环境、保留状态、接收反馈、协作收敛的运行基座。
        </p>
        <p>
            换句话说，作者并不满足于“代码生成”这个视角，而是提出一个更系统的判断：<strong>代码正在成为 agent system 的 harness</strong>。
            这里的 harness 可以理解成“支架”“承载层”或“运行框架”。它不是模型参数本身，也不是纯自然语言提示词，而是一层可执行、可检查、可维护的中介，把模型能力和现实任务环境接了起来。
        </p>

        <figure class="mt-6 bg-white/70 border border-white/70 rounded-2xl p-4">
            <img src="/notes/code-as-agent-harness/x1.png" alt="Figure 1: Code as Agent Harness overview" class="w-full rounded-xl" loading="lazy" />
            <figcaption class="text-sm text-gray-500 mt-3">Figure 1. 论文给出的总体图景：代码作为 agent harness，被拆成接口层、机制层和多智能体扩展层。</figcaption>
        </figure>

        <div class="note-box text-sm mt-4">
            <p><strong>原文摘录：</strong></p>
            <p class="text-sm text-gray-600 italic">“code is not only an artifact generated by LLMs, but increasingly the executable and inspectable medium through which agents reason, act, and adapt.”</p>
            <p class="text-xs text-gray-500 mt-2">来源：<a href="https://arxiv.org/html/2605.18747" class="text-[color:var(--accent)] underline">Code as Agent Harness, arXiv HTML</a></p>
        </div>

        <p class="mt-4">
            这句话其实点中了一个很关键的系统变化：一旦 agent 不再只是回答问题，而是要持续操作终端、编辑仓库、运行测试、读 GUI、调用工具、与其他 agent 协同，
            那么“文本回答是否像样”已经不够了。系统更关心的是：<strong>这个 agent 是否有可执行的工作区、是否能把中间状态沉淀成工件、是否能通过确定性验证器判断自己走得对不对、是否能在长流程中保持一致性</strong>。
            代码正是满足这些条件的最佳介质之一。
        </p>
    </div>

    <div class="card">
        <h2>2. Background &amp; Motivation：为什么普通“代码生成”视角已经不够了？</h2>
        <p>
            如果任务只是“写一段函数”或“补一段脚本”，那把代码当输出当然足够；但 agent 任务通常不是一次性的短答，而是一个跨多个步骤、多个工具、多个状态切换的长程过程。
            例如修复仓库 bug，agent 需要先理解 issue，再定位文件、检索上下文、修改代码、运行测试、观察报错、继续迭代；例如 GUI/OS agent，它要把界面理解、点击、输入、页面跳转、文件保存这些动作编织成一条可执行轨迹；
            再例如科学发现 agent，它不只生成实验想法，还要真正编排实验、收集结果、分析数据、更新结论。
        </p>
        <p>
            一旦系统变成这样，纯自然语言链路就会暴露三个老问题：
        </p>
        <ul class="list-disc pl-6 text-gray-600 space-y-2">
            <li><strong>不可执行：</strong>文本计划本身不能运行，必须再翻译成动作或程序。</li>
            <li><strong>不可检验：</strong>模型“觉得自己对了”和系统“有客观证据表明它对了”是两回事。</li>
            <li><strong>不易持久化：</strong>长流程中的状态、约束、失败原因、可复用技能如果只存在上下文窗口里，很难沉淀成真正可复用的资产。</li>
        </ul>
        <p class="mt-4">
            论文因此提出一个非常重要的转向：与其继续把 agent 看成“会说话的模型”，不如把它看成“在代码支架上运行的系统”。
            这也是为什么作者把代码定义成一种 <strong>operational substrate</strong>——它不只是表达结果，而是承载行为本身。
        </p>

        <figure class="mt-6 bg-white/70 border border-white/70 rounded-2xl p-4">
            <img src="/notes/code-as-agent-harness/x2.png" alt="Figure 2: Interface between reasoning, acting, and environment" class="w-full rounded-xl" loading="lazy" />
            <figcaption class="text-sm text-gray-500 mt-3">Figure 2. 代码把推理、行动与环境建模连接成一个统一接口，而不是只出现在“最终答案”那一步。</figcaption>
        </figure>

        <div class="overflow-x-auto mt-6">
            <table class="min-w-full divide-y divide-gray-200 text-sm">
                <thead class="bg-gray-50">
                    <tr>
                        <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">视角</th>
                        <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">代码扮演什么角色</th>
                        <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">系统最关心什么</th>
                    </tr>
                </thead>
                <tbody class="bg-white divide-y divide-gray-200 text-gray-600">
                    <tr>
                        <td class="px-4 py-3 font-medium text-slate-700">传统代码生成</td>
                        <td class="px-4 py-3">最终输出物</td>
                        <td class="px-4 py-3">能否一次生成正确补丁/函数/脚本</td>
                    </tr>
                    <tr>
                        <td class="px-4 py-3 font-medium text-slate-700">Code as Agent Harness</td>
                        <td class="px-4 py-3">运行时支架、共享状态与验证接口</td>
                        <td class="px-4 py-3">能否让推理、执行、反馈、记忆与协作形成闭环</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>

    <div class="card">
        <h2>3. 论文主张：一个三层框架，而不是一个单点算法</h2>
        <p>
            这篇论文不是提出一条新的 decoding 算法，也不是给出一个新的 benchmark，而是做了一次非常系统的框架化综述。作者把“代码作为 harness”拆成三层：
        </p>
        <ol class="list-decimal pl-6 text-gray-600 space-y-2">
            <li><strong>Harness Interface：</strong>代码如何把模型和任务世界接起来——也就是推理、行动、环境建模怎样通过代码落地。</li>
            <li><strong>Harness Mechanisms：</strong>代码进入闭环之后，规划、记忆、工具、执行—验证控制、优化机制怎样维持长程可靠运行。</li>
            <li><strong>Scaling the Harness：</strong>当系统升级为多智能体时，多个 agent 如何围绕共享代码状态协作，而不是各说各话。</li>
        </ol>
        <p class="mt-4">
            这三层很像软件系统里的三个视角：第一层处理“接口”，第二层处理“运行机制”，第三层处理“分布式协作”。
            这也是这篇综述最有价值的地方：它不再把 agent 论文拆成零散的 planning、memory、tool use、multi-agent、GUI agent，而是把它们重新纳入一条贯通的系统主线。
        </p>
        <div class="grid grid-cols-1 gap-6 md:grid-cols-3 mt-6">
            <div class="rounded-2xl border border-white/70 bg-white/80 p-5">
                <h3 class="text-lg font-semibold text-slate-800">Layer 1：Interface</h3>
                <p class="text-sm text-gray-600 mt-2">回答“agent 如何接入世界”。代码让推理可执行、行动可编程、环境可表示。</p>
            </div>
            <div class="rounded-2xl border border-white/70 bg-white/80 p-5">
                <h3 class="text-lg font-semibold text-slate-800">Layer 2：Mechanisms</h3>
                <p class="text-sm text-gray-600 mt-2">回答“接入以后如何稳定运行”。规划、记忆、工具、验证和遥测共同组成治理闭环。</p>
            </div>
            <div class="rounded-2xl border border-white/70 bg-white/80 p-5">
                <h3 class="text-lg font-semibold text-slate-800">Layer 3：Scaling</h3>
                <p class="text-sm text-gray-600 mt-2">回答“一个 agent 不够时怎样协作扩展”。共享代码状态、执行反馈和收敛标准是关键。</p>
            </div>
        </div>
        <div class="note-box text-sm mt-6">
            <p><strong>一个非常有用的阅读方法：</strong>把这篇论文当成“agent 系统版的操作系统教材目录”。它并不是教你写某个 agent，而是在问：一个真正可运行、可治理的 agent system 需要哪些基础设施层。</p>
        </div>
    </div>

    <div class="card">
        <h2>4. 一个形式化视角：为什么“可执行、可检查、有状态”这么关键？</h2>
        <p>
            论文本身是综述，不以公式为主；但为了理解它的系统主张，我们可以把 harness 抽象成一个概念模型。这个抽象不是论文原式，而是把论文里的系统思想压缩成一个更易操作的表示：
        </p>
        <div class="math-block" set:html={String.raw`$$
\mathcal{H} = (\mathcal{I}, \mathcal{S}, \mathcal{M}, \mathcal{T}, \mathcal{V}, \mathcal{C})
$$`}></div>
        <p class="text-sm text-gray-600 mt-3">
            其中，<span set:html={String.raw`$\mathcal{I}$`}></span> 表示接口（interfaces），<span set:html={String.raw`$\mathcal{S}$`}></span> 表示共享或持久状态（state），<span set:html={String.raw`$\mathcal{M}$`}></span> 表示记忆（memory），<span set:html={String.raw`$\mathcal{T}$`}></span> 表示工具与环境入口（tools / task environment），<span set:html={String.raw`$\mathcal{V}$`}></span> 表示验证器（verifiers），<span set:html={String.raw`$\mathcal{C}$`}></span> 表示控制与治理规则（control / constraints）。
        </p>
        <p>
            在这个抽象里，agent 并不是“想一想然后说一句话”，而是在 harness 上做一系列状态转移：
        </p>
        <div class="math-block" set:html={String.raw`$$
a_t \sim \pi_\theta(o_t, m_t, s_t; \mathcal{H}), \qquad s_{t+1} = T(s_t, a_t, e_t)
$$`}></div>
        <div class="math-block" set:html={String.raw`$$
v_t = V(s_t, a_t, s_{t+1}), \qquad m_{t+1} = U(m_t, s_{t+1}, v_t)
$$`}></div>
        <p class="mt-4">
            这里的含义是：给定观测 <span set:html={String.raw`$o_t$`}></span>、记忆 <span set:html={String.raw`$m_t$`}></span> 和当前状态 <span set:html={String.raw`$s_t$`}></span>，智能体在 harness 约束下产生动作 <span set:html={String.raw`$a_t$`}></span>；
            这个动作会改变系统状态，并且被验证器 <span set:html={String.raw`$V$`}></span> 评估；新的状态和验证结果再被写回记忆。
            <strong>代码之所以重要，是因为它同时参与了动作表示、状态持久化、验证触发与记忆沉淀</strong>。
        </p>
        <p>
            这正是“code as harness”和“natural language agent loop”最大的区别：自然语言可以描述计划，但代码更容易承担“可执行状态转移”的角色。
            它使系统从“我认为我完成了”转向“我有测试、日志、轨迹、工件证明我完成了”。
        </p>
    </div>

    <div class="card">
        <h2>5. Method / System Part I：Harness Interface——代码如何成为推理、行动、环境的接口</h2>
        <p>
            第一层是整篇论文最基础也最重要的一层。作者认为，代码之所以能够成为 harness，不只是因为它能被模型写出来，而是因为它能把三件本来分散的事情统一起来：<strong>推理、行动、环境建模</strong>。
            这三者一旦通过代码接上，agent 的行为就不再漂浮在 prompt 中，而是落在了一个可执行的世界里。
        </p>

        <figure class="mt-6 bg-white/70 border border-white/70 rounded-2xl p-4">
            <img src="/notes/code-as-agent-harness/x3.png" alt="Figure 3: Interface roadmap" class="w-full rounded-xl" loading="lazy" />
            <figcaption class="text-sm text-gray-500 mt-3">Figure 3. 论文梳理的接口层路线图：代码分别作为推理、行动与环境表示的接口。</figcaption>
        </figure>

        <h3 class="text-teal-700 mt-8">5.1 Code for Reasoning：把“思考”外化成程序执行</h3>
        <p>
            在最早的 CoT 范式里，模型通过自然语言中间步骤完成推理；但一旦涉及算术、多步符号推导、约束求解或形式证明，纯文本中间过程很容易变成“像推理的文字”。
            代码接口改变了这点：模型不必自己在语言里完成所有细节，而是可以把某部分推理委托给程序、解释器、求解器或形式系统。
        </p>
        <p>
            论文在这一部分强调了三个典型路径：第一，<strong>program-delegated reasoning</strong>，也就是让模型产出程序，再由解释器完成真正的计算；第二，<strong>formal verification / symbolic reasoning interface</strong>，把 Lean、SAT/SMT、proof assistant 等形式系统作为推理闭环的一部分；第三，<strong>iterative code-grounded reasoning</strong>，让模型在“生成—执行—验证—修订”的回路中逐步靠近正确答案。
        </p>
        <p>
            这个视角最重要的启发是：<strong>代码不是推理后的附赠物，而是推理本身的一部分</strong>。
            当系统可以运行程序、读取变量、观察异常、比较输出时，“中间过程正确性”就不再全靠模型自我表述，而是能被外部执行证据校准。
        </p>

        <h3 class="text-teal-700 mt-8">5.2 Code for Acting：把高层意图转成可执行动作</h3>
        <p>
            对很多 agent 来说，代码的意义不在于“算”，而在于“做”。例如 GUI/OS agent 需要把“打开设置页面并修改选项”变成一系列 API 调用、DOM 操作、鼠标键盘事件；机器人 agent 需要把“抓起杯子并放到托盘上”分解为技能选择、动作约束与控制程序；代码 agent 则要把“修复 issue”转换成终端命令、文件编辑、测试运行与 git 操作。
        </p>
        <p>
            论文把这一层分为三类：<strong>grounded skill selection</strong>、<strong>programmatic policy generation</strong> 与 <strong>lifelong code-based agents</strong>。
            第一类强调从已有技能中检索并组合动作；第二类进一步让模型直接生成策略代码或行为树；第三类则把成功经验沉淀为未来可复用的技能库。它们共同说明：代码在行动层既是执行指令，也是能力积累的媒介。
        </p>

        <h3 class="text-teal-700 mt-8">5.3 Code for Environment：把环境本身变成可执行对象</h3>
        <p>
            这也许是整篇论文最容易被低估、但最有系统意味的一点：代码不仅表示 agent 的动作，也能表示 agent 所处的世界。仓库结构、测试脚本、模拟器、日志、轨迹、配置文件、评测器，这些都不是“外部噪声”，而是环境的一部分。
        </p>
        <p>
            一旦环境是程序化的，agent 就能在其中读取状态、触发变更、生成新环境、甚至验证自己是否达成目标。SWE-bench 之所以重要，不只是因为它是一个 benchmark，而是因为它把真实软件问题放进了一个<strong>受约束、可复现、可执行验证</strong>的环境里；
            同样，InterCode、AgentBench 以及许多 GUI/OS 环境的重要性，也都来自“任务不是靠主观打分完成，而是靠环境反馈决定”。
        </p>

        <div class="overflow-x-auto mt-6">
            <table class="min-w-full divide-y divide-gray-200 text-sm">
                <thead class="bg-gray-50">
                    <tr>
                        <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">接口方向</th>
                        <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">代码承担的职责</th>
                        <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">代表性例子</th>
                    </tr>
                </thead>
                <tbody class="bg-white divide-y divide-gray-200 text-gray-600">
                    <tr>
                        <td class="px-4 py-3 font-medium text-slate-700">Reasoning</td>
                        <td class="px-4 py-3">把推理外化为程序、求解器或形式证明过程</td>
                        <td class="px-4 py-3">PAL、PoT、DeepSeek-Prover 等</td>
                    </tr>
                    <tr>
                        <td class="px-4 py-3 font-medium text-slate-700">Acting</td>
                        <td class="px-4 py-3">把高层目标转换成技能调用、策略代码与控制逻辑</td>
                        <td class="px-4 py-3">SayCan、Voyager、GUI/OS agents</td>
                    </tr>
                    <tr>
                        <td class="px-4 py-3 font-medium text-slate-700">Environment</td>
                        <td class="px-4 py-3">把仓库、测试、模拟器、配置、轨迹变成环境状态</td>
                        <td class="px-4 py-3">SWE-bench、InterCode、OSWorld</td>
                    </tr>
                </tbody>
            </table>
        </div>

        <div class="note-box text-sm mt-6">
            <p><strong>关键理解：</strong>普通“代码生成”把代码放在链路末端；而 interface 视角把代码放在链路中间，让它同时接住推理、动作和环境三件事。</p>
        </div>
    </div>

    <div class="card">
        <h2>6. Method / System Part II：Harness Mechanisms——代码进入闭环之后，系统靠什么稳定运行？</h2>
        <p>
            接口层回答的是“如何接入世界”，机制层回答的是“接进来以后如何长期运行”。这部分可以看作整篇论文最接近工程实践的地方：它把今天大家耳熟能详的 planning、memory、tool use、verification、optimization 全都重新解释成 harness 机制，而不是零散外挂。
        </p>

        <figure class="mt-6 bg-white/70 border border-white/70 rounded-2xl p-4">
            <img src="/notes/code-as-agent-harness/x4.png" alt="Figure 4: Harness mechanisms overview" class="w-full rounded-xl" loading="lazy" />
            <figcaption class="text-sm text-gray-500 mt-3">Figure 4. 机制层总览：规划、记忆、工具、Plan-Execute-Verify 与自适应优化共同组成 agent harness 的运行机制。</figcaption>
        </figure>

        <h3 class="text-teal-700 mt-8">6.1 Planning：规划不是附加提示，而是执行轨迹控制器</h3>
        <p>
            论文把规划分成线性分解、结构约束、搜索式和编排式四类。这个分类很重要，因为它说明“规划”并不只是让模型先列一个 TODO list。
            在 harness 视角里，规划更像一种 <strong>execution contract</strong>：它决定接下来该碰哪些文件、以什么顺序做验证、什么阶段能升级权限、失败后回退到哪里。
        </p>
        <p>
            这也是为什么现代 coding agent 往往不只是输出“Step 1, Step 2, Step 3”，而是要把计划和工作区状态、工具权限、验证门槛挂钩。
            计划如果不能影响实际状态转移，那它仍然只是语言层的装饰；只有当计划成为执行约束，它才真正属于 harness。
        </p>

        <figure class="mt-6 bg-white/70 border border-white/70 rounded-2xl p-4">
            <img src="/notes/code-as-agent-harness/x5.png" alt="Figure 5: Planning mechanisms" class="w-full rounded-xl" loading="lazy" />
            <figcaption class="text-sm text-gray-500 mt-3">Figure 5. 规划机制的分层：从线性分解到结构约束、搜索与编排式规划。</figcaption>
        </figure>

        <h3 class="text-teal-700 mt-8">6.2 Memory：记忆不是“更长上下文”，而是状态管理系统</h3>
        <p>
            这篇论文对记忆的处理非常值得注意。很多讨论会把 memory 简化成“多一个向量库”或“多一个 long context”，但作者把它拆成工作记忆、语义记忆、经验记忆、长期记忆、多智能体记忆以及上下文压缩与状态卸载。
            这说明在真实 agent 系统里，记忆的本质不是“多存一点”，而是<strong>决定什么应当留在当前回路里，什么应当持久化，什么应当压缩，什么又应该被检索回来</strong>。
        </p>
        <p>
            这点在 repo-scale agent 上尤为明显。代码库、测试日志、变更 diff、运行结果、人工审批意见，这些都不可能永远塞在 prompt 里；如果没有状态分层，agent 很快就会失去上下文结构，只剩下“把上一轮对话总结成一段文字”的脆弱补丁。
        </p>

        <figure class="mt-6 bg-white/70 border border-white/70 rounded-2xl p-4">
            <img src="/notes/code-as-agent-harness/x6.png" alt="Figure 6: Memory and context engineering" class="w-full rounded-xl" loading="lazy" />
            <figcaption class="text-sm text-gray-500 mt-3">Figure 6. 记忆与上下文工程：从工作记忆到长期记忆，再到多智能体共享记忆与上下文压缩。</figcaption>
        </figure>

        <h3 class="text-teal-700 mt-8">6.3 Tool Use：工具不是外挂，而是受治理的动作接口</h3>
        <p>
            论文把工具使用分成四类：面向函数/API 的工具、面向环境交互的工具、面向验证的工具以及面向工作流编排的工具。这个拆法的意义在于，它把“工具调用”从一个 API 级别的问题，提升成了“系统控制面”的问题。
        </p>
        <p>
            例如一个终端工具不仅仅意味着“能执行命令”，还意味着会产生新文件、改变工作目录、影响后续验证；一个浏览器工具不只是“能打开页面”，而是会引入新的 DOM 状态、cookie、会话与截图证据；一个测试工具也不只是“跑一下 pytest”，而是在给系统提供确定性传感器。
            因此，真正成熟的 harness 不只是把工具名暴露给模型，而是要定义输入 schema、输出结构、权限边界、失败语义和日志回放方式。
        </p>

        <figure class="mt-6 bg-white/70 border border-white/70 rounded-2xl p-4">
            <img src="/notes/code-as-agent-harness/x7.png" alt="Figure 7: Tool usage mechanisms" class="w-full rounded-xl" loading="lazy" />
            <figcaption class="text-sm text-gray-500 mt-3">Figure 7. 工具使用不只是 API 调用，而是知识获取、环境交互、验证与工作流编排的统一接口。</figcaption>
        </figure>

        <h3 class="text-teal-700 mt-8">6.4 Plan–Execute–Verify：执行反馈比自我反思更重要</h3>
        <p>
            论文把 PEV（Plan–Execute–Verify）写成机制层中的核心控制回路，这一点非常贴近真实工程。因为 agent 的关键问题从来不是“它会不会反思”，而是“它有没有客观证据知道自己错了”。
            对代码任务而言，编译器、测试框架、静态分析器、类型检查器、fuzzer、性能 profiler，都是天然的验证器。
        </p>
        <p>
            这使得 code harness 与纯语言 agent 最大的不同再一次显现：<strong>代码环境能够产出确定性的错误信号</strong>。
            一旦系统把这些信号接入控制回路，agent 就可以不依赖“语义上看起来对”，而是以“执行上被证伪 / 被证成”为依据继续修复。也正因此，SWE-bench 这类环境比单纯 judge 打分更有基础设施意义。
        </p>

        <figure class="mt-6 bg-white/70 border border-white/70 rounded-2xl p-4">
            <img src="/notes/code-as-agent-harness/x8.png" alt="Figure 8: Plan Execute Verify" class="w-full rounded-xl" loading="lazy" />
            <figcaption class="text-sm text-gray-500 mt-3">Figure 8. Plan–Execute–Verify 闭环：计划形成契约，执行发生在沙箱中，验证由确定性传感器完成。</figcaption>
        </figure>

        <div class="note-box text-sm mt-4">
            <p><strong>实践映射：</strong>像 OpenHands 这类系统把终端、文件、工作区和执行环境组织成统一工作台；像 SWE-bench 这样的环境则进一步提供“是否真正解决实例”的客观标准。两者结合起来，正是 code-as-harness 的现实雏形。</p>
        </div>

        <h3 class="text-teal-700 mt-8">6.5 Agentic Harness Engineering：未来不仅调模型，还要调 harness</h3>
        <p>
            机制层最前沿的一块，是作者称作 <strong>Agentic Harness Engineering</strong> 的方向。它的核心不是让 agent 优化业务代码，而是让 agent 优化“自己赖以工作的那套支架”：提示模板、工具 schema、上下文组织、验证顺序、角色编排、权限门控、遥测与回滚策略。
        </p>
        <p>
            这个方向很值得单独记住，因为它意味着未来的性能瓶颈可能不在模型参数，而在系统壳层。
            当不同 coding agent 都能调用同样的基础模型时，真正拉开差距的，很可能就是哪一家的 harness 更擅长收集轨迹、约束状态迁移、减少无效尝试、放大执行反馈，并在不破坏安全边界的前提下持续进化。
        </p>

        <figure class="mt-6 bg-white/70 border border-white/70 rounded-2xl p-4">
            <img src="/notes/code-as-agent-harness/x9.png" alt="Figure 9: Agentic Harness Engineering" class="w-full rounded-xl" loading="lazy" />
            <figcaption class="text-sm text-gray-500 mt-3">Figure 9. Agentic Harness Engineering：把深度遥测、失败分析、受治理的变更与回归验证纳入 harness 自身的优化回路。</figcaption>
        </figure>
    </div>

    <div class="card">
        <h2>7. Method / System Part III：Scaling the Harness——多智能体真正共享的不是对话，而是代码状态</h2>
        <p>
            多智能体系统经常被描述成“一个 planner、一个 coder、一个 reviewer、一个 tester”，但这篇论文进一步追问：这些 agent 到底围绕什么协作？
            作者的答案非常明确：<strong>围绕共享的代码中心状态</strong>。换句话说，多智能体协作不是多几段对话，而是多角色在同一套仓库、工件、验证器与反馈流上进行协调。
        </p>

        <figure class="mt-6 bg-white/70 border border-white/70 rounded-2xl p-4">
            <img src="/notes/code-as-agent-harness/x10.png" alt="Figure 10: Multi-agent extension overview" class="w-full rounded-xl" loading="lazy" />
            <figcaption class="text-sm text-gray-500 mt-3">Figure 10. 多智能体扩展：角色分工、执行反馈整合、共享状态同步与收敛标准共同构成代码中心的协作系统。</figcaption>
        </figure>

        <h3 class="text-teal-700 mt-8">7.1 角色分工：专门化是为了把不同失败模式拆开</h3>
        <p>
            论文梳理了 synthesis、understanding、verification、execution、planning 等不同角色。表面看这是常识，实则非常关键：在长流程任务里，“没想到正确方案”“修改了错误文件”“跑错了验证命令”“没看懂日志”和“没有在正确时机停下来请求人工审批”是完全不同的失败。
            单体 agent 很容易把这些失败混在一起，最后只表现为“它又没做对”；多智能体的价值之一，就是把这些失败模式显式拆开，并给每个部分配上更适合的输入、工具和验证口径。
        </p>

        <h3 class="text-teal-700 mt-8">7.2 交互模式：协作、批评、对抗与辩论，本质上都在消费共享状态</h3>
        <p>
            论文提到 collaborative synthesis、critique-and-repair、adversarial validation、reasoning debate 等交互模式。很多人会把它们理解为“多 agent 对话花样”，但从 harness 视角看，更本质的点在于：这些交互之所以有意义，是因为它们都以共享代码状态为对象。
            批评不是抽象地说“你不对”，而是指向某个文件、某个测试、某种失败轨迹；辩论也不是抽象表态，而是围绕仓库事实、执行结果和约束条件形成局部共识。
        </p>

        <h3 class="text-teal-700 mt-8">7.3 共享状态表示：从 file-only 到 execution-aware，是这篇论文最强的系统判断之一</h3>
        <p>
            我认为这一节是整篇综述里最值得反复咀嚼的段落。作者指出，大部分多智能体系统虽然表面上在“共享工作区”，但实际上共享状态仍然停留在极其隐式的层次：要么共享文件，要么共享聊天记录，要么共享一段摘要。
            这远远不够，因为真实协作需要的不只是文件快照，还包括执行上下文、验证历史、权限边界、计划依赖与记忆索引。
        </p>
        <p>
            作者因此区分了四种共享表示：<strong>implicit / file-only</strong>、<strong>repository-based</strong>、<strong>execution-based</strong> 和 <strong>blackboard / shared-state</strong>。
            从这个角度看，今天许多 agent 系统其实还处在“文件共享”阶段，距离真正的 transaction-like shared harness 还有不小距离。
        </p>

        <figure class="mt-6 bg-white/70 border border-white/70 rounded-2xl p-4">
            <img src="/notes/code-as-agent-harness/x11.png" alt="Figure 11: Multi-agent roadmap" class="w-full rounded-xl" loading="lazy" />
            <figcaption class="text-sm text-gray-500 mt-3">Figure 11. 多智能体代码 harness 路线图：从角色分工延伸到执行反馈整合、共享状态同步和收敛标准。</figcaption>
        </figure>

        <div class="overflow-x-auto mt-6">
            <table class="min-w-full divide-y divide-gray-200 text-sm">
                <thead class="bg-gray-50">
                    <tr>
                        <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">共享层级</th>
                        <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">能共享什么</th>
                        <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">主要缺陷</th>
                    </tr>
                </thead>
                <tbody class="bg-white divide-y divide-gray-200 text-gray-600">
                    <tr>
                        <td class="px-4 py-3 font-medium text-slate-700">File-only</td>
                        <td class="px-4 py-3">文件内容与 diff</td>
                        <td class="px-4 py-3">缺少执行上下文、验证历史与权限语义</td>
                    </tr>
                    <tr>
                        <td class="px-4 py-3 font-medium text-slate-700">Repository-based</td>
                        <td class="px-4 py-3">目录结构、依赖、提交状态</td>
                        <td class="px-4 py-3">仍然不够细粒度，不反映运行时状态</td>
                    </tr>
                    <tr>
                        <td class="px-4 py-3 font-medium text-slate-700">Execution-based</td>
                        <td class="px-4 py-3">日志、测试、崩溃轨迹、性能信号</td>
                        <td class="px-4 py-3">需要更强的结构化存储与同步机制</td>
                    </tr>
                    <tr>
                        <td class="px-4 py-3 font-medium text-slate-700">Shared-state / Blackboard</td>
                        <td class="px-4 py-3">计划、证据、约束、状态依赖、执行记录</td>
                        <td class="px-4 py-3">实现复杂，需要事务化与语义级冲突处理</td>
                    </tr>
                </tbody>
            </table>
        </div>

        <p class="mt-6">
            这张表能帮助我们理解一个现实问题：为什么很多多智能体系统 agent 数量越多，效果不一定越好？
            原因很简单——如果共享状态没有被正式化，更多角色只会制造更多同步负担。
            因此作者进一步提出 <strong>harness-state convergence</strong> 的概念：最终收敛不应只靠“跑了三轮就停”或者“大家投票说差不多”，而应该尽量由正确性、安全性、性能与测试信号决定。
        </p>
    </div>

    <div class="card">
        <h2>8. Experiments / Evaluation：这篇综述没有统一实验，但给出了更强的评测视角</h2>
        <p>
            由于这是综述而不是单一方法论文，它没有像算法论文那样提供一个统一实验表格；但它并没有回避评测问题，反而把“如何评估 harness”提升为全文最核心的开放议题之一。
            在作者看来，传统只看最终成功率的评估方式，已经不足以判断 agent harness 的好坏。
        </p>
        <p>
            为什么？因为 harness 的价值不只是把任务做完，而是体现在整个执行过程：是否能用更少试错达到成功、是否能在失败时可靠恢复、是否能正确保存状态、是否能避免越权、是否能把验证证据完整沉淀下来。
            这些能力都很难被一个单独的“pass / fail”数字概括。
        </p>
        <p>
            也正因此，作者把像 SWE-bench、InterCode、AgentBench、LiveCodeBench、OSWorld 等环境放在了非常关键的位置。它们的重要性不只是“这是热门榜单”，而是因为它们把 agent 的表现绑定到可执行环境上。
            比如 SWE-bench 官方展示的核心指标就是 <em>% Resolved</em>，也就是实例被真实解决的比例；这恰好对应论文强调的方向：<strong>代码任务的评测应该更多依赖环境中的可复现结果，而不是纯文本 judge</strong>。
        </p>
        <div class="note-box text-sm mt-4">
            <p><strong>补充来源理解：</strong>SWE-bench 之所以经常出现在代码 agent 讨论中，不只是因为它难，而是因为它把“真实仓库 + 可执行验证”变成了评测基础设施。这与论文所说的 code-grounded evaluation environment 是完全一致的。</p>
        </div>
        <p class="mt-4">
            从这个角度出发，PEV 闭环其实也可以看作一种评测哲学：与其问“模型会不会自我反思”，不如问“它是否真正纳入了编译器、测试器、静态分析器、仿真器、性能 profiler 等确定性传感器”。
            前者是主观自述，后者是客观反馈；前者可能让 agent 在语言里显得很聪明，后者才真正决定它能不能在系统里工作。
        </p>
    </div>

    <div class="card">
        <h2>9. Emerging Fields：为什么这不是“只对写代码有用”的观点？</h2>
        <p>
            论文最后一大块内容，是把 code-as-harness 视角扩展到多个新兴领域。这里最重要的启发是：<strong>代码作为 agent harness 不是软件工程特例，而是一种更普遍的系统组织原则</strong>。
        </p>

        <figure class="mt-6 bg-white/70 border border-white/70 rounded-2xl p-4">
            <img src="/notes/code-as-agent-harness/x12.png" alt="Figure 12: Applications and emerging fields" class="w-full rounded-xl" loading="lazy" />
            <figcaption class="text-sm text-gray-500 mt-3">Figure 12. 论文总结的五类新兴应用：代码助手、GUI/OS agent、具身智能体、科学发现 agent 与个性化系统。</figcaption>
        </figure>

        <h3 class="text-teal-700 mt-8">9.1 代码助手：仓库是工作区，验证器是裁判</h3>
        <p>
            这部分最贴近今天的 Claude Code、OpenHands、Codex、Copilot coding agents 等系统。它们的共同点是：模型不再只生成一个代码片段，而是在一个完整工作区中持续操作。
            终端、文件系统、测试框架、浏览器、issue、提交历史、项目约定，共同构成了 agent 的任务世界。
            这正是“代码作为 harness”最直观的现实版本。
        </p>
        <p>
            OpenHands 文档首页其实就体现了这一点：它不是把自己定义为“聊天助手”，而是定义为统一的软件代理工作区，底层是可组合的 SDK，上层则提供 CLI、GUI、Cloud 与 Enterprise 形态。
            这与论文的判断高度一致——真正关键的不是模型能不能补一段代码，而是系统是否能围绕真实开发流程组织工作。
        </p>

        <h3 class="text-teal-700 mt-8">9.2 GUI / OS agent：图形界面其实也是一种程序世界</h3>
        <p>
            论文把 GUI/OS agent 放在 code-as-harness 视角下重新解释，意思并不是说界面本身变成了“代码”，而是说 GUI agent 的可观察对象和可行动对象都可以被程序化表示。
            DOM、AXTree、截图、坐标、控件属性、事件轨迹、本地文件、浏览器状态，这些都可以成为 agent 的结构化环境。于是 GUI agent 的任务也从“看懂一张图”变成“在一个程序世界中执行动作并接收反馈”。
        </p>

        <h3 class="text-teal-700 mt-8">9.3 具身智能体：技能、约束与控制程序成为长期状态</h3>
        <p>
            对具身 agent 来说，代码并不只是离线脚本，而是连接高层目标和底层控制边界的媒介。技能库、行为树、动作约束、任务脚本、仿真器配置，都能被程序化和持久化。
            这样一来，机器人不只是“用语言规划动作”，而是在代码支架上调度已习得技能，并将成功经验继续沉淀成长期资产。
        </p>

        <h3 class="text-teal-700 mt-8">9.4 科学发现：实验流程天然适合被程序世界化</h3>
        <p>
            科学发现 agent 是这篇论文里我个人非常喜欢的一块，因为它说明了 harness 视角的外延。假设生成、实验编排、数据分析、结果整理、论文撰写，这一整条链路其实都可以落在程序化工件上。
            一旦实验室设备、模拟器、分析脚本和文献检索都纳入同一支架，agent 就不再是在“帮研究者写摘要”，而是在真正参与一个可执行的科研工作流。
        </p>

        <h3 class="text-teal-700 mt-8">9.5 个性化：偏好状态不应该永远藏在隐向量里</h3>
        <p>
            个性化领域的启发则在于：用户偏好是一种持续变化的状态，如果它始终只存在模型内部的隐含表示里，那么系统很难让用户编辑、审计、纠错。
            论文因此把个性化也纳入 harness 视角：偏好画像、历史反馈、可解释约束、记忆摘要，都应当尽量成为可访问、可治理的程序化状态，而不只是一个黑盒 embedding。
        </p>
    </div>

    <div class="card">
        <h2>10. Discussion：这篇论文最值得带走的 6 个洞察</h2>

        <h3 class="text-teal-700 mt-6">10.1 代码的价值不止在“表达能力”，更在“状态能力”</h3>
        <p>
            很多讨论会强调代码比自然语言更精确、更结构化，但这篇论文更进一步指出：代码真正重要的地方在于它能够承载状态。文件、配置、日志、测试、脚本、依赖、轨迹都可以留下来，并在下一轮被重新消费。
            这让 agent 的行为不再被困在一次对话中，而是能变成持续存在的工件系统。
        </p>

        <h3 class="text-teal-700 mt-6">10.2 执行反馈是语言推理与形式推理之间的桥</h3>
        <p>
            过去很多 agent 失败，根源并不是“模型不够聪明”，而是系统没有给它足够强的外部反馈。代码环境的特别之处就在于，编译错误、测试失败、静态分析告警、性能回归、仿真偏差都能作为客观证据进入回路。
            这使得语言模型第一次能大规模地与“形式上的正确性信号”对齐。
        </p>

        <h3 class="text-teal-700 mt-6">10.3 多智能体的真正瓶颈不是通信，而是共享状态过于隐式</h3>
        <p>
            这也是论文对多智能体最重要的批评之一。只要共享状态仍停留在“看看同一份文件”“把上轮结果总结成一段文字”，多智能体扩展就会很脆弱。
            真正稳健的多智能体系统，需要更像数据库、事务系统或协作编辑系统那样思考共享状态与冲突消解。
        </p>

        <h3 class="text-teal-700 mt-6">10.4 Harness 的质量，最终取决于治理而不是炫技</h3>
        <p>
            论文不断强调 sandbox、permissioned transition、deterministic sensors、governed mutation 等概念。它们听起来不如“agent 自我进化”那样吸睛，却更接近现实。
            因为在生产环境里，一个能够持续跑通任务、留下完整证据、在高风险操作前停下来请求审批的 agent，通常比一个“看起来更会思考”的 agent 更有价值。
        </p>

        <h3 class="text-teal-700 mt-6">10.5 未来的竞争很可能发生在 harness 层，而不只是模型层</h3>
        <p>
            随着基础模型能力逐渐趋同，谁更擅长组织工作区、切分任务、管理记忆、聚合反馈、约束权限、压缩上下文、做回归验证，谁就更可能做出真正可靠的 agent 系统。
            从这个意义上说，<strong>agent engineering 正在变成 harness engineering</strong>。
        </p>

        <h3 class="text-teal-700 mt-6">10.6 这篇论文其实在呼吁建立一门“agent 系统科学”</h3>
        <p>
            最后的开放问题部分，作者谈到 harness-level evaluation、semantic verification、non-regressive self-evolution、transactional shared state、human-in-the-loop accountability、multimodal harness 等议题。
            这些问题共同指向一个更大的命题：如果 agent 不再是玩具 demo，而是要成为长期运行的系统，那么我们需要的已不只是“更强模型”，而是一套围绕代码支架建立起来的系统科学。
        </p>
    </div>

    <div class="card">
        <h2>11. Open Problems：作者点出的难题，为什么都很硬？</h2>
        <p>
            论文最后列出的开放问题非常值得认真看，因为它们不是“未来还有提升空间”这类泛泛结论，而是直接点出现阶段 agent system 最难啃的系统问题。
        </p>
        <ul class="list-disc pl-6 text-gray-600 space-y-3">
            <li><strong>Harness-level evaluation：</strong>最终成功率远远不够，需要评估轨迹效率、恢复能力、验证强度、状态一致性与安全性。</li>
            <li><strong>Semantic verification beyond execution：</strong>“能跑通”并不总等于“真正正确”，尤其在业务语义、隐含约束和长尾边界上更是如此。</li>
            <li><strong>Non-regressive self-evolution：</strong>让 harness 自我优化很诱人，但如何避免引入隐性回归、安全弱化和偏向局部任务的过拟合，是极难的问题。</li>
            <li><strong>Transactional shared state：</strong>多智能体不仅会发生文本冲突，还会发生计划冲突、记忆冲突、验证冲突和权限冲突。</li>
            <li><strong>Human-in-the-loop accountability：</strong>人在回路中不该只是一句“请确认”，而应该成为一种持久的、可审计的 harness 状态。</li>
            <li><strong>Multimodal harness：</strong>图像、视频、传感器、桌面界面、机器人控制等多模态状态如何被压缩、验证和共享，仍然缺乏统一范式。</li>
        </ul>
        <p class="mt-4">
            这些难题也解释了为什么“更强模型”并不会自动变成“更强 agent”。模型只是 harness 中的一个组件；而真正困难的地方，是如何把模型放进一条可执行、可验证、可状态化、可治理的链路里。
        </p>
    </div>

    <div class="card">
        <h2>12. 实践建议：如果你今天在做 agent 系统，这篇论文最可落地的建议是什么？</h2>
        <p>
            如果把这篇综述压缩成今天就能用的工程建议，我会总结成下面六条：
        </p>
        <ol class="list-decimal pl-6 text-gray-600 space-y-2">
            <li><strong>把工作区当作一等公民。</strong> 终端、文件系统、测试环境、浏览器、日志都应显式建模，而不是藏在黑箱中。</li>
            <li><strong>让计划能约束执行。</strong> 计划不应只是语言摘要，而应绑定目标文件、验证步骤、风险边界与回滚点。</li>
            <li><strong>优先接入确定性验证器。</strong> 编译器、测试框架、静态分析器和 profiler 的价值通常高于一轮新的自我反思。</li>
            <li><strong>把记忆做成状态分层，而不是无限长上下文。</strong> 工作记忆、语义记忆、经验记忆和长期记忆应该有不同职责。</li>
            <li><strong>多智能体之前，先想清楚共享状态。</strong> 如果只有对话转述，没有结构化状态层，更多 agent 往往只会带来更多噪音。</li>
            <li><strong>从第一天开始做遥测。</strong> 没有日志、轨迹、工具参数、验证结果和审批记录，就谈不上真正的 harness optimization。</li>
        </ol>
        <p class="mt-4">
            从这个角度看，所谓“agent 产品化”本质上不是把模型 API 包一层 UI，而是把代码、状态、验证、权限和回放能力组织成一套可治理的运行时。
        </p>
    </div>

    <div class="card">
        <h2>13. Conclusion：从“模型会写代码”到“代码组织了智能体”</h2>
        <p>
            <em>Code as Agent Harness</em> 最有价值的地方，不是提出一个新术语，而是把很多正在同时发生、却常被分开讨论的趋势重新缝合到了一起：代码辅助推理、代码驱动行动、代码表示环境、基于执行反馈的验证、repo-scale memory、多智能体协作、GUI/OS automation、科学工作流 agent、个性化代理……
            当这些现象放到一起看，就会发现它们背后其实共享一个更深的判断：<strong>代码正在从“模型输出”变成“agent 运行时基础设施”</strong>。
        </p>
        <p>
            这个判断非常重要，因为它把 agent 研究的关注点从“模型能否生成正确内容”转向“系统能否围绕可执行工件形成可靠闭环”。
            在这样的世界里，真正决定 agent 是否有用的，不再只是生成能力，而是它能否利用代码支架承接计划、状态、验证、反馈、协作与治理。
        </p>
        <p>
            如果说过去几年我们学会了让模型“写代码”，那这篇论文提醒我们：下一阶段更难、也更有意义的事情，是让代码真正成为 agent 的 harness。
        </p>
    </div>

    <div class="card">
        <h2>14. Original Figures</h2>
        <p>以下为论文原图的本地收录，便于快速回看整体结构与章节对应关系。</p>
        <div class="grid grid-cols-1 gap-6 md:grid-cols-2 mt-6">
            <figure class="rounded-2xl border border-white/70 bg-white/80 p-4">
                <img src="/notes/code-as-agent-harness/x1.png" alt="Figure 1" class="w-full rounded-xl" loading="lazy" />
                <figcaption class="text-sm text-gray-500 mt-3">Figure 1. Code as Agent Harness 总体分类框架。</figcaption>
            </figure>
            <figure class="rounded-2xl border border-white/70 bg-white/80 p-4">
                <img src="/notes/code-as-agent-harness/x2.png" alt="Figure 2" class="w-full rounded-xl" loading="lazy" />
                <figcaption class="text-sm text-gray-500 mt-3">Figure 2. 代码作为推理、行动、环境接口。</figcaption>
            </figure>
            <figure class="rounded-2xl border border-white/70 bg-white/80 p-4">
                <img src="/notes/code-as-agent-harness/x3.png" alt="Figure 3" class="w-full rounded-xl" loading="lazy" />
                <figcaption class="text-sm text-gray-500 mt-3">Figure 3. 接口层路线图。</figcaption>
            </figure>
            <figure class="rounded-2xl border border-white/70 bg-white/80 p-4">
                <img src="/notes/code-as-agent-harness/x4.png" alt="Figure 4" class="w-full rounded-xl" loading="lazy" />
                <figcaption class="text-sm text-gray-500 mt-3">Figure 4. 机制层总览。</figcaption>
            </figure>
            <figure class="rounded-2xl border border-white/70 bg-white/80 p-4">
                <img src="/notes/code-as-agent-harness/x5.png" alt="Figure 5" class="w-full rounded-xl" loading="lazy" />
                <figcaption class="text-sm text-gray-500 mt-3">Figure 5. 规划机制。</figcaption>
            </figure>
            <figure class="rounded-2xl border border-white/70 bg-white/80 p-4">
                <img src="/notes/code-as-agent-harness/x6.png" alt="Figure 6" class="w-full rounded-xl" loading="lazy" />
                <figcaption class="text-sm text-gray-500 mt-3">Figure 6. 记忆与上下文工程。</figcaption>
            </figure>
            <figure class="rounded-2xl border border-white/70 bg-white/80 p-4">
                <img src="/notes/code-as-agent-harness/x7.png" alt="Figure 7" class="w-full rounded-xl" loading="lazy" />
                <figcaption class="text-sm text-gray-500 mt-3">Figure 7. 工具使用机制。</figcaption>
            </figure>
            <figure class="rounded-2xl border border-white/70 bg-white/80 p-4">
                <img src="/notes/code-as-agent-harness/x8.png" alt="Figure 8" class="w-full rounded-xl" loading="lazy" />
                <figcaption class="text-sm text-gray-500 mt-3">Figure 8. Plan–Execute–Verify 控制回路。</figcaption>
            </figure>
            <figure class="rounded-2xl border border-white/70 bg-white/80 p-4">
                <img src="/notes/code-as-agent-harness/x9.png" alt="Figure 9" class="w-full rounded-xl" loading="lazy" />
                <figcaption class="text-sm text-gray-500 mt-3">Figure 9. Agentic Harness Engineering。</figcaption>
            </figure>
            <figure class="rounded-2xl border border-white/70 bg-white/80 p-4">
                <img src="/notes/code-as-agent-harness/x10.png" alt="Figure 10" class="w-full rounded-xl" loading="lazy" />
                <figcaption class="text-sm text-gray-500 mt-3">Figure 10. 多智能体扩展总览。</figcaption>
            </figure>
            <figure class="rounded-2xl border border-white/70 bg-white/80 p-4">
                <img src="/notes/code-as-agent-harness/x11.png" alt="Figure 11" class="w-full rounded-xl" loading="lazy" />
                <figcaption class="text-sm text-gray-500 mt-3">Figure 11. 多智能体代码 harness 路线图。</figcaption>
            </figure>
            <figure class="rounded-2xl border border-white/70 bg-white/80 p-4">
                <img src="/notes/code-as-agent-harness/x12.png" alt="Figure 12" class="w-full rounded-xl" loading="lazy" />
                <figcaption class="text-sm text-gray-500 mt-3">Figure 12. 代码作为 harness 的应用图景。</figcaption>
            </figure>
        </div>
    </div>

    <div class="card">
        <h2>15. 参考资料（Primary Sources &amp; Supplementary Sources）</h2>
        <ul class="list-disc pl-6 mb-4 space-y-2 text-gray-600">
            <li><a class="text-teal-700 underline" href="https://arxiv.org/abs/2605.18747" target="_blank" rel="noopener noreferrer">Code as Agent Harness (arXiv Abstract)</a> —— 原论文摘要页，理解论文定位与核心论点的第一手来源。</li>
            <li><a class="text-teal-700 underline" href="https://arxiv.org/html/2605.18747" target="_blank" rel="noopener noreferrer">Code as Agent Harness (arXiv HTML)</a> —— 原论文全文 HTML，本文的大部分结构、图示与开放问题都来自这里。</li>
            <li><a class="text-teal-700 underline" href="https://github.com/YennNing/Awesome-Code-as-Agent-Harness-Papers" target="_blank" rel="noopener noreferrer">Awesome Code as Agent Harness Papers</a> —— 作者提供的配套 GitHub 资源库，帮助理解该主题的分层分类与延伸阅读路线。</li>
            <li><a class="text-teal-700 underline" href="https://www.swebench.com/" target="_blank" rel="noopener noreferrer">SWE-bench</a> —— 代表性的代码约束评测环境，适合理解“可执行验证环境”为何是 agent harness 的核心组成。</li>
            <li><a class="text-teal-700 underline" href="https://docs.openhands.dev/" target="_blank" rel="noopener noreferrer">OpenHands Documentation</a> —— 现代软件代理工作区的官方文档示例，可帮助把论文中的 harness 视角映射到现实产品形态。</li>
        </ul>
    </div>
</BaseLayout>
