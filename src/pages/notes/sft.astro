---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { todayYmd } from '../../utils/date';

const createdAt = todayYmd();
---

<BaseLayout title="SFT：Supervised Fine-Tuning">
    <header class="text-center mb-12">
        <p class="text-xs uppercase tracking-[0.3em] text-[color:var(--muted)]">SFT / Fine-tuning / Alignment</p>
        <h1 class="text-4xl font-bold mb-4 gradient-text">Supervised Fine-Tuning (SFT)</h1>
        <p class="text-xl text-gray-500">LLM 对齐的核心技术：从训练目标到数据策略</p>
        <p class="mt-4 text-sm uppercase tracking-[0.2em] text-[color:var(--muted)]">添加时间 {createdAt} • 10 min</p>
    </header>

    <div class="card">
        <h2>什么是 SFT？</h2>
        <p>Supervised Fine-Tuning (SFT) 是训练大型语言模型的关键步骤，在预训练模型的基础上，使用人工标注的指令-响应对数据集进行进一步训练，使模型的输出更符合人类期望的行为和能力。</p>
    </div>

    <div class="card">
        <h2>背景</h2>
        <p>在大规模无标注文本上进行预训练后，LLM 具备了通用的语言理解能力，但缺乏指令跟随能力，可能产生不够有用或不安全的输出。SFT 通过以下方式弥补这一差距：</p>
        <ul class="list-disc pl-6 mt-4 space-y-2">
            <li><strong>有用性 (Helpful)</strong>：准确且相关地回答用户问题</li>
            <li><strong>诚实性 (Honest)</strong>：承认不确定性和局限性</li>
            <li><strong>无害性 (Harmless)</strong>：避免有毒或危险内容</li>
        </ul>
    </div>

    <div class="card">
        <h2>训练目标</h2>
        <p>SFT 使用交叉熵损失函数，衡量模型预测的 token 分布与训练数据中标准响应之间的差异：</p>
        <div class="mt-4 bg-slate-100 rounded-xl p-4 text-center">
            <p set:html={"$\\mathcal{L}_{SFT} = -\\sum_{i=1}^{N} \\sum_{t=1}^{T_i} \\log p_\\theta(y_t^i | y_{<t}^i, x^i)$"}></p>
        </div>
        <p class="mt-4 text-sm text-gray-600">其中：</p>
        <ul class="list-disc pl-6 mt-2 space-y-1 text-sm text-gray-600">
            <li>$\theta$ 是模型参数</li>
            <li>$x^i$ 是输入提示</li>
            <li>$y^i$ 是目标响应</li>
            <li>$N$ 是训练样本数量</li>
        </ul>
    </div>

    <div class="card">
        <h2>数据收集</h2>
        <p>高质量的 SFT 数据集通常包括：</p>
        <ol class="list-decimal pl-6 mt-4 space-y-3">
            <li><strong>人工演示</strong>：标注员编写理想的响应</li>
            <li><strong>人类偏好数据</strong>：从多个候选中选出最佳响应</li>
            <li><strong>合成数据</strong>：模型生成的响应经人工优化</li>
        </ol>
    </div>

    <div class="card">
        <h2>训练细节</h2>
        <ul class="list-disc pl-6 space-y-2">
            <li><strong>Epochs</strong>：通常 1-3 个 epoch 以避免过拟合</li>
            <li><strong>学习率</strong>：常用 1e-5 到 5e-6，带 warmup</li>
            <li><strong>Batch size</strong>：小批量 (4-16)，受 GPU 内存限制</li>
            <li><strong>序列长度</strong>：完整响应长度，通常 4K-8K tokens</li>
        </ul>
    </div>

    <div class="card">
        <h2>实践建议</h2>
        <ol class="list-decimal pl-6 space-y-3">
            <li><strong>数据质量 > 数据量</strong>：高质量小数据集通常优于低质量大数据集</li>
            <li><strong>多样性很重要</strong>：覆盖各种任务类型和领域</li>
            <li><strong>响应长度</strong>：在简洁性和完整性之间取得平衡</li>
            <li><strong>避免能力退化</strong>：SFT 可能导致某些能力下降</li>
        </ol>
    </div>

    <div class="card">
        <h2>讨论</h2>
        <p>SFT 仍然是对齐 LLM 的主流方法，但也存在局限性：</p>
        <ul class="list-disc pl-6 mt-4 space-y-2">
            <li><strong>可扩展性</strong>：人工标注成本高昂</li>
            <li><strong>泛化能力</strong>：模型可能无法泛化到未见过任务</li>
            <li><strong>奖励黑客</strong>：可能产生表面模式被"破解"</li>
        </ul>
    </div>

    <div class="card">
        <h2>结论</h2>
        <p>Supervised Fine-Tuning 是 LLM 开发中的关键步骤，将基础模型转化为有用的、遵循指令的助手。结合 RLHF 和 DPO，形成了现代 LLM 对齐流程的核心。</p>
    </div>
</BaseLayout>
