---
import BaseLayout from '../../layouts/BaseLayout.astro';

export const meta = {
    title: 'AgentSPEX：用 YAML 把 Agent 工作流写成可执行规范',
    description: '深入解读 AgentSPEX 论文：声明式 Agent 工作流语言、显式状态、子模块复用、执行 harness、可观测性、评测结果与工程边界。',
    tags: ['Agent', 'Workflow', 'DSL', 'Systems'],
    readTime: '42 min',
    createdAt: '2026-05-28'
};
---

<BaseLayout title="AgentSPEX：用 YAML 把 Agent 工作流写成可执行规范" description="从反应式提示词到声明式工作流：系统理解 AgentSPEX 的语言设计、执行 harness、评测结果与工程启发。">
    <header class="text-center mb-12">
        <p class="text-xs uppercase tracking-[0.3em] text-[color:var(--muted)]">Agent Systems / Workflow DSL / Execution Harness</p>
        <h1 class="text-4xl font-bold mb-4 gradient-text">AgentSPEX：用 YAML 把 Agent 工作流写成可执行规范</h1>
        <p class="text-xl text-gray-500">从“让模型自由反应”到“让控制流、状态与工具调用被显式编排”</p>
        <p class="mt-4 text-sm uppercase tracking-[0.2em] text-[color:var(--muted)]">添加时间 {meta.createdAt}</p>
    </header>

    <div class="note-box text-sm">
        <p><strong>论文信息：</strong><em>AgentSPEX: An Agent SPecification and EXecution Language</em>，arXiv:2604.13346v1，2026-04-14。</p>
        <p class="mt-2"><strong>核心定位：</strong>AgentSPEX 不是一个新模型，也不是一个新训练目标，而是一套面向 LLM Agent 的声明式工作流规范语言与执行 harness。它把 Agent 的控制流、状态、子模块、并发、工具访问、检查点和日志从“模型上下文里的隐含过程”提升为“可读、可编辑、可执行、可版本控制的工程对象”。</p>
    </div>

    <nav class="toc mt-8" aria-label="目录">
        <h2>目录</h2>
        <ul>
            <li><a href="#intro">1. Introduction：AgentSPEX 要解决什么问题</a></li>
            <li><a href="#background">2. Background：ReAct、Python 编排与声明式规范的边界</a></li>
            <li><a href="#language">3. Language：AgentSPEX 工作流语言如何表达 Agent</a></li>
            <li><a href="#state">4. State &amp; Composition：显式状态、上下文窗口与子模块复用</a></li>
            <li><a href="#harness">5. Agent Harness：解释器、执行器、沙箱与工具调用闭环</a></li>
            <li><a href="#durability">6. Observability &amp; Durability：日志、检查点、回放与验证</a></li>
            <li><a href="#evaluation">7. Experiments：七个 benchmark 与用户研究说明了什么</a></li>
            <li><a href="#practice">8. Practice：真实系统里应该如何使用这种 DSL</a></li>
            <li><a href="#limits">9. Discussion：局限、误区与未来方向</a></li>
            <li><a href="#figures">10. Original Figures &amp; References</a></li>
        </ul>
    </nav>

    <div class="card" id="intro">
        <h2>1. Introduction：AgentSPEX 要解决什么问题</h2>
        <p>
            过去两年里，Agent 框架的发展有一条很清楚的主线：从“给模型一段系统提示词，让它自己决定下一步”逐渐走向“把任务拆成可管理的状态机、图、工具链和子模块”。
            ReAct 风格的 Agent 很灵活，模型可以根据观察结果临时决定下一次思考、搜索、编辑、运行测试或调用工具；但这种灵活性也带来一个工程上的代价：
            <span class="highlight">控制流藏在对话历史里，状态藏在自然语言里，执行语义藏在模型行为里</span>。
            当任务变长、工具变多、团队想复用同一个 Agent 时，这些隐含结构会让系统难以调试、难以复现，也难以让非框架作者参与维护。
        </p>
        <p>
            论文 <em>AgentSPEX: An Agent SPecification and EXecution Language</em> 的回答是：把 Agent 工作流写成一种声明式规范。
            这里的 SPEX 来自 <strong>SPecification and EXecution</strong>，也就是说，作者希望同一份 YAML 文件既是人能读懂的设计文档，又是机器可以执行的工作流程序。
            它让开发者显式写出：哪些步骤开启新对话，哪些步骤继承上下文；哪些变量应该保存和传递；什么时候分支、循环或并行；怎样调用另一个 workflow；工具和子模块在每一步能否使用；执行失败后如何恢复。
        </p>

        <figure class="mt-6 bg-white/70 border border-white/70 rounded-2xl p-4">
            <img src="/notes/agentspex/figure-1-system-architecture.png" alt="Figure 1: AgentSPEX architecture" class="w-full rounded-xl" loading="lazy" />
            <figcaption class="text-sm text-gray-500 mt-3">Figure 1. AgentSPEX 总体架构：工作流规范、可视化编辑器、解释执行 harness、沙箱工具环境、日志与可恢复执行共同构成一套 Agent 工程化闭环。</figcaption>
        </figure>

        <div class="note-box text-sm mt-4">
            <p><strong>原文摘录：</strong></p>
            <p class="text-sm text-gray-600 italic">“Language-model agent systems commonly rely on reactive prompting, in which a single instruction guides the model through an open-ended sequence of reasoning and tool-use steps, leaving control flow and intermediate state implicit and making agent behavior potentially difficult to control.”</p>
            <p class="text-xs text-gray-500 mt-2">来源：<a href="https://arxiv.org/html/2604.13346v1" class="text-[color:var(--accent)] underline">AgentSPEX arXiv HTML</a> Abstract</p>
        </div>

        <p class="mt-4">
            这句话解释了整篇论文的动机：Agent 的问题不只是“模型会不会推理”，更是“系统能不能把推理、行动、状态、工具和恢复机制组织成一个可控过程”。
            在短任务里，单一 prompt 足够；在长任务里，Agent 会遇到分支、循环、重试、上下文腐化、工具调用失败、部分结果复用、人工检查和多模块组合。
            AgentSPEX 的价值不在于让模型突然更聪明，而在于把这些长流程的结构交给解释器和 harness 管理，让模型专注于每个局部步骤里的语言理解、计划生成和工具使用。
        </p>
    </div>

    <div class="card" id="background">
        <h2>2. Background：ReAct、Python 编排与声明式规范的边界</h2>
        <p>
            理解 AgentSPEX，最好先把它放在三类 Agent 编排方式之间比较。第一类是 <strong>reactive prompting</strong>：给模型一个目标、若干工具描述和历史上下文，模型在每轮输出中自行决定是否继续推理或调用工具。
            这种方式实现成本最低，也是很多通用 Agent 的默认形态。它的弱点是：如果一个任务本来就需要固定的阶段划分、明确的检查点或严格的上下文隔离，模型每次都要重新“理解并遵守”这套流程。
            流程越长，越容易出现跳步、重复、把无关历史带入当前步骤、工具调用失控或成本不可预测。
        </p>
        <p>
            第二类是 Python 编排框架，例如论文中提到的 LangGraph、DSPy、CrewAI。它们把 Agent 写成图、pipeline 或多角色团队，能表达复杂控制流，也能和普通程序逻辑深度集成。
            但代价是工作流逻辑与 Python 代码紧耦合：prompt、状态结构、分支条件、工具配置、日志和恢复逻辑常常散落在代码里。
            对框架工程师来说这很自然；对领域专家、产品同学或只想微调一个流程的人来说，修改成本会明显上升。
        </p>
        <p>
            第三类就是 AgentSPEX 代表的声明式规范路线。它不是完全不要代码，而是把“工作流是什么”从“工作流如何由 Python 执行”中分离出来。
            YAML 文件负责描述 Agent 的可读结构，执行 harness 负责解释它、调用模型和工具、维护上下文、保存检查点。
            这与 Kubernetes YAML、GitHub Actions workflow、Airflow DAG 或 CI 配置的工程思想相似：开发者不需要每次重写调度器，只需要声明任务拓扑和每个节点的行为。
        </p>

        <div class="overflow-x-auto mt-6">
            <table class="min-w-full divide-y divide-gray-200 text-sm">
                <thead class="bg-gray-50">
                    <tr>
                        <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">方式</th>
                        <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">控制流在哪里</th>
                        <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">优势</th>
                        <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">主要风险</th>
                    </tr>
                </thead>
                <tbody class="bg-white divide-y divide-gray-200 text-gray-600">
                    <tr>
                        <td class="px-4 py-3 font-medium text-slate-700">ReAct / reactive prompting</td>
                        <td class="px-4 py-3">模型上下文与历史消息</td>
                        <td class="px-4 py-3">简单、灵活、启动成本低</td>
                        <td class="px-4 py-3">流程不可显式验证，长任务中上下文和步骤容易漂移</td>
                    </tr>
                    <tr>
                        <td class="px-4 py-3 font-medium text-slate-700">Python orchestration</td>
                        <td class="px-4 py-3">Python 代码、图结构、类和函数</td>
                        <td class="px-4 py-3">表达力强，容易接入复杂系统</td>
                        <td class="px-4 py-3">prompt 与控制逻辑紧耦合，非程序员难维护</td>
                    </tr>
                    <tr>
                        <td class="px-4 py-3 font-medium text-slate-700">AgentSPEX</td>
                        <td class="px-4 py-3">声明式 YAML workflow</td>
                        <td class="px-4 py-3">可读、可 diff、可视化、显式状态、可恢复执行</td>
                        <td class="px-4 py-3">复杂逻辑仍可能需要代码/工具支持，DSL 语义也需要学习</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>

    <div class="card" id="language">
        <h2>3. Language：AgentSPEX 工作流语言如何表达 Agent</h2>
        <p>
            AgentSPEX 的工作流文件有几个稳定的顶层字段：<code>name</code> 表示工作流标识，<code>goal</code> 表示高层目标，<code>parameters</code> 提供输入参数和默认值，<code>config</code> 配置模型、工具、token 或调用限制，<code>workflow</code> 则是一组按顺序解释的操作。
            这些字段看起来很像普通配置文件，但真正关键的是：<code>workflow</code> 里的每个节点都有执行语义，而不只是注释。
        </p>

        <figure class="mt-6 bg-white/70 border border-white/70 rounded-2xl p-4">
            <figcaption class="text-sm text-gray-500 mb-3">Figure 2. 论文中的 topic research and summarization 工作流示例，展示 AgentSPEX 如何用 YAML 表达生成搜索 query、调用搜索总结子模块、再写报告的三段式流程。</figcaption>
            <pre class="overflow-x-auto rounded-xl bg-slate-950 p-4 text-xs text-slate-100"><code>{`name: "research_assistant"
goal: "Research a topic and write a summary"
config:
  model: "gpt-5.4"
  enabled_tools: ["web_search", "file_write"]
parameters:
  topic: "Enhancing LLM reasoning via RLHF"
  file_path: "outputs/report.md"

workflow:
  - task:
      instruction: "Generate a list of search queries for {{topic}}"
      save_as: "search_queries"
  - call:
      module: "modules/search_and_summarize.yaml"
      parameters:
        queries: "{{search_queries}}"
      save_as: "paper_summary"
  - task:
      instruction: "Write a report at {{file_path}} based on these findings: {{paper_summary}}"`}</code></pre>
        </figure>

        <h3 class="mt-8">3.1 两个最重要的调用原语：task 与 step</h3>
        <p>
            论文把 LLM 调用拆成 <code>task</code> 和 <code>step</code>，这是 AgentSPEX 里最容易被低估的设计。
            <code>task</code> 会开启新的对话，不继承前面完整的聊天历史；如果它需要前序结果，必须通过变量显式传入。
            <code>step</code> 则延续共享会话历史，适合多轮交互、持续补充指令或保留模型对同一问题的上下文。
            这相当于把“上下文窗口管理”从模型的默认行为变成了 workflow 作者的显式选择。
        </p>
        <div class="grid grid-cols-1 gap-6 md:grid-cols-2 mt-6">
            <div class="rounded-2xl border border-white/70 bg-white/80 p-5">
                <h3 class="text-lg font-semibold text-slate-800"><code>task</code>：新会话、显式输入</h3>
                <p class="text-sm text-gray-600 mt-2">适合独立分析、批处理、并行 workers 或需要避免历史噪声的步骤。它让工作流更像函数调用：输入来自参数和变量，输出通过 <code>save_as</code> 保存。</p>
            </div>
            <div class="rounded-2xl border border-white/70 bg-white/80 p-5">
                <h3 class="text-lg font-semibold text-slate-800"><code>step</code>：共享会话、连续推理</h3>
                <p class="text-sm text-gray-600 mt-2">适合需要同一模型会话持续维护上下文的多轮任务，例如逐步修订报告、连续观察工具结果、在一个推理链中补充约束。</p>
            </div>
        </div>

        <h3 class="mt-8">3.2 控制流、组合、并发与状态</h3>
        <p>
            AgentSPEX 的语言构造覆盖了长流程 Agent 中最常见的模式。<code>if</code> 和 <code>switch</code> 处理条件分支；<code>while</code> 与 <code>for_each</code> 处理循环；<code>call</code> 调用另一个 workflow；<code>parallel</code> 与 <code>gather</code> 执行并发；<code>set_variable</code>、<code>increment</code>、<code>input</code> 和 <code>return</code> 管理状态与返回值。
            这些构造不像通用编程语言那样试图覆盖所有计算，而是专门服务于 Agent 运行：什么地方需要 LLM，什么地方只需要控制器判断，什么地方可以并发，什么地方应该保存结果。
        </p>
        <div class="overflow-x-auto mt-6">
            <table class="min-w-full divide-y divide-gray-200 text-sm">
                <thead class="bg-gray-50">
                    <tr>
                        <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">构造</th>
                        <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">类别</th>
                        <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">工程含义</th>
                    </tr>
                </thead>
                <tbody class="bg-white divide-y divide-gray-200 text-gray-600">
                    <tr><td class="px-4 py-3 font-medium text-slate-700"><code>task</code> / <code>step</code></td><td class="px-4 py-3">Invocation</td><td class="px-4 py-3">决定是否继承对话上下文，控制 LLM 调用边界。</td></tr>
                    <tr><td class="px-4 py-3 font-medium text-slate-700"><code>if</code> / <code>switch</code></td><td class="px-4 py-3">Control flow</td><td class="px-4 py-3">把条件判断交给解释器，而不是让模型自由决定是否进入某个阶段。</td></tr>
                    <tr><td class="px-4 py-3 font-medium text-slate-700"><code>while</code> / <code>for_each</code></td><td class="px-4 py-3">Iteration</td><td class="px-4 py-3">支持迭代改进、列表处理和可设置上限的长流程。</td></tr>
                    <tr><td class="px-4 py-3 font-medium text-slate-700"><code>call</code></td><td class="px-4 py-3">Composition</td><td class="px-4 py-3">把另一个 YAML workflow 当成子模块或技能复用。</td></tr>
                    <tr><td class="px-4 py-3 font-medium text-slate-700"><code>parallel</code> / <code>gather</code></td><td class="px-4 py-3">Concurrency</td><td class="px-4 py-3">让搜索、评审、批量分析等任务并发执行并汇总结果。</td></tr>
                    <tr><td class="px-4 py-3 font-medium text-slate-700"><code>set_variable</code> / <code>return</code></td><td class="px-4 py-3">State</td><td class="px-4 py-3">让中间结果成为可命名、可传递、可检查的状态。</td></tr>
                </tbody>
            </table>
        </div>
    </div>

    <div class="card" id="state">
        <h2>4. State &amp; Composition：显式状态、上下文窗口与子模块复用</h2>
        <p>
            AgentSPEX 的状态模型很朴素：每个 workflow 维护一组命名上下文变量，步骤通过 <code>save_as</code> 保存输出，后续步骤用 Mustache 风格的 <code>{`{{variable}}`}</code> 模板引用。
            朴素并不等于不重要。长程 Agent 的很多失败并不是模型完全不会做，而是中间状态没有被清楚地命名、裁剪和传递。
            如果所有信息都靠完整对话历史携带，后续步骤会同时看到目标、草稿、错误尝试、工具噪声和过期假设；如果把关键状态保存成变量，workflow 作者就可以决定每一步只接收它真正需要的上下文。
        </p>
        <p>
            这也解释了论文为什么强调“explicit conversation history management”。在 AgentSPEX 中，工作流作者可以选择用 <code>task</code> 丢弃历史、只传入摘要变量；也可以用 <code>step</code> 保留持续对话；还可以把大型任务拆成多个 <code>call</code> 子模块，让每个模块有自己的局部上下文。
            这种结构对成本也有影响：并不是每个步骤都需要吃下完整上下文，也不是每个 worker 都需要知道主流程的所有历史。
        </p>

        <figure class="mt-6 rounded-2xl border border-white/70 bg-white/80 p-4">
            <svg viewBox="0 0 920 330" role="img" aria-label="AgentSPEX state and context flow" class="w-full">
                <rect x="24" y="24" width="872" height="282" rx="22" fill="#f8fafc" stroke="#e2e8f0" />
                <text x="50" y="58" font-size="14" fill="#64748b">显式状态流：不是把所有历史都推给模型，而是让变量成为上下文边界</text>
                <rect x="54" y="92" width="190" height="78" rx="14" fill="#dbeafe" stroke="#bfdbfe" />
                <text x="149" y="124" text-anchor="middle" font-size="13" fill="#0f172a">task: find_sources</text>
                <text x="149" y="148" text-anchor="middle" font-size="11" fill="#475569">save_as: sources</text>
                <path d="M244 130 H330" stroke="#38bdf8" stroke-width="3" marker-end="url(#arrow)" />
                <rect x="330" y="92" width="190" height="78" rx="14" fill="#dcfce7" stroke="#bbf7d0" />
                <text x="425" y="124" text-anchor="middle" font-size="13" fill="#0f172a">for_each source</text>
                <text x="425" y="148" text-anchor="middle" font-size="11" fill="#475569">task: summarize</text>
                <path d="M520 130 H606" stroke="#22c55e" stroke-width="3" marker-end="url(#arrow)" />
                <rect x="606" y="92" width="220" height="78" rx="14" fill="#fef3c7" stroke="#fde68a" />
                <text x="716" y="124" text-anchor="middle" font-size="13" fill="#0f172a">task: synthesize_report</text>
                <text x="716" y="148" text-anchor="middle" font-size="11" fill="#475569">inputs: summaries only</text>
                <rect x="150" y="220" width="620" height="46" rx="12" fill="#ffffff" stroke="#e2e8f0" />
                <text x="460" y="249" text-anchor="middle" font-size="12" fill="#475569">context = {`{topic, sources, source_summaries, final_report}`}</text>
                <defs>
                    <marker id="arrow" markerWidth="10" markerHeight="10" refX="8" refY="3" orient="auto" markerUnits="strokeWidth">
                        <path d="M0,0 L0,6 L9,3 z" fill="#64748b" />
                    </marker>
                </defs>
            </svg>
            <figcaption class="text-sm text-gray-500 mt-3">显式变量让每一步拥有更清楚的输入输出边界，也让回放、检查点和验证可以围绕命名状态展开。</figcaption>
        </figure>

        <h3 class="mt-8">4.1 子模块统一抽象：workflow 既是 Agent，也是技能</h3>
        <p>
            论文的第二个关键设计是统一子模块抽象：任何 workflow 都可以通过 <code>call</code> 被另一个 workflow 调用；某些 workflow 还可以声明成 function-like submodule，暴露给 LLM 作为可调用工具。
            这使得“技能”“子 Agent”“复杂 pipeline”不再是三套完全不同的机制，而是同一种 YAML 工作流在不同层级上的复用。
            例如 deep research 主流程可以调用 search-and-summarize 子模块，AI Scientist 可以调用 citation worker，软件工程 Agent 可以调用定位、补丁生成、测试验证等子流程。
        </p>
        <p>
            这里真正重要的是模块边界。一个子模块可以有自己的工具过滤、自己的参数、自己的返回变量和自己的检查点；父流程只关心它的输入输出。
            这让 Agent 系统更接近软件工程里的组件化：你可以单独调试某个模块，也可以把它作为稳定能力接入更大的流程。
        </p>
    </div>

    <div class="card" id="harness">
        <h2>5. Agent Harness：解释器、执行器、沙箱与工具调用闭环</h2>
        <p>
            如果只有 YAML 语法，AgentSPEX 只是一个配置格式；论文真正把它变成系统的是 Agent Harness。
            Harness 的入口是 interpreter：它读取 workflow 文件，校验结构，解析参数，展开模板变量，然后按顺序遍历 <code>workflow</code> 操作，把不同节点分派给对应 handler。
            当遇到 <code>if</code>、<code>while</code>、<code>for_each</code>、<code>parallel</code> 或 <code>call</code> 时，解释器负责维护执行栈、作用域、循环上限和层级式 step id。
        </p>
        <p>
            LLM 与工具的交互由 executor 负责。它把当前消息历史发送给模型；如果模型返回工具调用，就通过 Model Context Protocol（MCP）客户端执行工具，把工具结果追加回消息历史；如此循环，直到模型不再调用工具，或者达到工具调用数、token 数等限制。
            这个循环听起来类似普通 tool-use Agent，但差异在于：它被工作流节点包裹，每个节点的输入、可用工具、上下文历史和输出保存位置都是显式的。
        </p>
        <p>
            执行环境则使用 Docker-based sandbox。论文与官方仓库都提到，工作流运行在隔离容器中，可以访问浏览器、文件系统、Web 搜索、代码执行、浏览器自动化等超过 50 类工具。
            这说明 AgentSPEX 面向的不是静态问答，而是需要真实环境交互的 Agent：写文件、跑脚本、查网页、操作浏览器、保存报告、修复代码都属于它的目标场景。
        </p>

        <div class="grid grid-cols-1 gap-6 md:grid-cols-3 mt-6">
            <div class="rounded-2xl border border-white/70 bg-white/80 p-5">
                <h3 class="text-lg font-semibold text-slate-800">Interpreter</h3>
                <p class="text-sm text-gray-600 mt-2">负责 YAML 解析、模板展开、控制流调度、作用域与 step id 管理。它把“流程是否执行到这里”从模型判断中拿出来。</p>
            </div>
            <div class="rounded-2xl border border-white/70 bg-white/80 p-5">
                <h3 class="text-lg font-semibold text-slate-800">Executor</h3>
                <p class="text-sm text-gray-600 mt-2">负责模型调用与工具调用循环，通过 MCP 客户端执行工具，并把观察结果写回当前步骤的会话。</p>
            </div>
            <div class="rounded-2xl border border-white/70 bg-white/80 p-5">
                <h3 class="text-lg font-semibold text-slate-800">Sandbox VM</h3>
                <p class="text-sm text-gray-600 mt-2">提供隔离运行环境与文件、搜索、浏览器、代码执行等工具，避免 Agent 直接污染宿主环境。</p>
            </div>
        </div>

        <div class="note-box text-sm mt-6">
            <p><strong>工程类比：</strong>如果把模型看成“会做局部判断的执行单元”，AgentSPEX 的 harness 就像一个专用调度器。调度器不替模型推理，但决定什么时候让模型推理、把哪些上下文交给模型、哪些工具可用、结果存到哪里、失败后从哪里恢复。</p>
        </div>
    </div>

    <div class="card" id="durability">
        <h2>6. Observability &amp; Durability：日志、检查点、回放与验证</h2>
        <p>
            长流程 Agent 的一个现实问题是：它们经常不是“一次跑完就结束”。API timeout、rate limit、网络故障、浏览器状态丢失、工具异常和模型输出不稳定都会让运行中断。
            如果没有持久化机制，开发者只能从头重跑；而从头重跑不仅浪费成本，也会因为 LLM 非确定性导致上游结果变化，让单点调试变得困难。
        </p>
        <p>
            AgentSPEX 的 durability 系统围绕两个机制展开。第一是 <strong>checkpointing</strong>：每个步骤完成后，harness 保存已完成 step id、当前上下文变量、先前步骤输出、步骤级指标和当前沙箱状态。
            恢复时，系统会还原上下文，跳过已完成步骤，并重新连接原沙箱会话。
            第二是 <strong>execution tracing and replay</strong>：harness 记录完整执行轨迹，包括模型响应、工具调用结果和每步会话状态；当开发者只修改某个步骤的 prompt 或控制逻辑时，可以复用上游轨迹，只重新执行后续步骤。
        </p>

        <figure class="mt-6 bg-white/70 border border-white/70 rounded-2xl p-4">
            <img src="/notes/agentspex/figure-4-dashboard.png" alt="Figure 4: AgentSPEX dashboard log viewer" class="w-full rounded-xl" loading="lazy" />
            <figcaption class="text-sm text-gray-500 mt-3">Figure 4. 论文附录中的 observability dashboard 示例：日志查看器展示 SWE-Bench Verified 实例执行过程中的步骤、上下文、工具输出、token 与成本信息。</figcaption>
        </figure>

        <h3 class="mt-8">6.1 为什么声明式 workflow 更容易验证</h3>
        <p>
            论文还讨论了 formal verification 的方向。由于 AgentSPEX 把控制流、变量依赖和步骤边界写成显式 YAML，系统可以对 workflow 结构和执行轨迹做静态或运行时检查。
            这不同于普通 Python Agent：Python 当然也能验证，但控制流可能嵌在任意函数、闭包、回调和框架内部，很难用统一方式枚举每个 Agent 步骤的前置条件和后置条件。
        </p>
        <div class="my-6 rounded-2xl border border-white/70 bg-white/80 p-5 text-center">
            <p class="text-sm text-gray-500">一个节点级验证可以抽象成：</p>
            <p class="mt-3 text-lg font-semibold text-slate-800">preconditions(stepᵢ, contextᵢ) ⇒ execute(stepᵢ) ⇒ postconditions(stepᵢ, contextᵢ₊₁)</p>
            <p class="mt-3 text-sm text-gray-600">其中 <code>contextᵢ</code> 是执行前的命名变量集合，<code>contextᵢ₊₁</code> 是执行后的变量集合。只要节点边界清楚，验证器就能检查文件路径是否有效、JSON schema 是否匹配、BibTeX 是否可解析、返回值是否非空等条件。</p>
        </div>
        <p>
            需要注意的是，论文目前把 formal verification 更多作为潜力展示，而不是完整验证框架的最终形态。附录展示了 citation extraction module 的计划和轨迹验证示例，例如检查 <code>file_path</code> 是否是有效路径、<code>paper_title</code> 是否非空字符串、<code>bibtex_citation</code> 是否有效 BibTeX、返回字典是否符合 schema。
            这说明声明式结构至少打开了一个方向：Agent 不再只是不可解释的对话流，而是可以被审计、断言和部分形式化的执行轨迹。
        </p>
    </div>

    <div class="card" id="evaluation">
        <h2>7. Experiments：七个 benchmark 与用户研究说明了什么</h2>
        <p>
            论文评测覆盖 science、mathematics、writing、scientific paper understanding 和 software engineering 五类任务，共七个 benchmark。
            对大多数 benchmark，作者比较了 CoT baseline、ReAct baseline 和 AgentSPEX workflow。这里的 ReAct baseline 很关键：它会把同一份 AgentSPEX workflow 放进初始提示词，让模型自行理解并反应式执行；而 AgentSPEX 则由 harness 强制逐步执行。
            因此这组对比不只是“有没有 workflow”，而是“workflow 是否由外部解释器强制执行”。
        </p>

        <div class="overflow-x-auto mt-6">
            <table class="min-w-full divide-y divide-gray-200 text-sm">
                <thead class="bg-gray-50">
                    <tr>
                        <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Benchmark</th>
                        <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Domain</th>
                        <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Best baseline</th>
                        <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">AgentSPEX</th>
                        <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">观察</th>
                    </tr>
                </thead>
                <tbody class="bg-white divide-y divide-gray-200 text-gray-600">
                    <tr><td class="px-4 py-3 font-medium text-slate-700">SciBench</td><td class="px-4 py-3">Science</td><td class="px-4 py-3">ReAct 87.79%</td><td class="px-4 py-3 font-semibold text-emerald-700">90.61%</td><td class="px-4 py-3">多步科学推理受益于结构化阶段。</td></tr>
                    <tr><td class="px-4 py-3 font-medium text-slate-700">StemEZ</td><td class="px-4 py-3">Science</td><td class="px-4 py-3">ReAct 84.72%</td><td class="px-4 py-3 font-semibold text-emerald-700">86.57%</td><td class="px-4 py-3">物理化学推导需要稳定的计算流程。</td></tr>
                    <tr><td class="px-4 py-3 font-medium text-slate-700">ChemBench</td><td class="px-4 py-3">Science</td><td class="px-4 py-3">CoT 78.90%</td><td class="px-4 py-3 font-semibold text-emerald-700">83.30%</td><td class="px-4 py-3">比强 baseline 高 5.5 个百分点，是增益较大的任务之一。</td></tr>
                    <tr><td class="px-4 py-3 font-medium text-slate-700">AIME 2025</td><td class="px-4 py-3">Mathematics</td><td class="px-4 py-3">CoT + Python 99.60%</td><td class="px-4 py-3 font-semibold text-emerald-700">100.0%</td><td class="px-4 py-3">数学任务接近饱和，更多体现流程可控性。</td></tr>
                    <tr><td class="px-4 py-3 font-medium text-slate-700">ELAIPBench</td><td class="px-4 py-3">Paper Understanding</td><td class="px-4 py-3">CoT 37.22%</td><td class="px-4 py-3 font-semibold text-emerald-700">43.70%</td><td class="px-4 py-3">全文论文理解受益于上下文切分和显式状态。</td></tr>
                    <tr><td class="px-4 py-3 font-medium text-slate-700">WritingBench</td><td class="px-4 py-3">Writing</td><td class="px-4 py-3">ReAct 80.30%</td><td class="px-4 py-3 font-semibold text-emerald-700">81.00%</td><td class="px-4 py-3">写作任务增益较小，说明单步语言能力仍是主因。</td></tr>
                    <tr><td class="px-4 py-3 font-medium text-slate-700">SWE-Bench Verified</td><td class="px-4 py-3">Software Engineering</td><td class="px-4 py-3">mini-SWE-agent 76.20%</td><td class="px-4 py-3 font-semibold text-emerald-700">77.10%</td><td class="px-4 py-3">平均 Claude-Opus-4.5/4.6，结构化 workflow 略优于专门 SWE agent。</td></tr>
                </tbody>
            </table>
        </div>

        <div class="note-box text-sm mt-6">
            <p><strong>原文摘录：</strong></p>
            <p class="text-sm text-gray-600 italic">“Overall results. As shown in Table 2, AgentSPEX achieves the highest score among the compared approaches on all 7 benchmarks.”</p>
            <p class="text-xs text-gray-500 mt-2">来源：<a href="https://arxiv.org/pdf/2604.13346v1" class="text-[color:var(--accent)] underline">AgentSPEX PDF</a> Section 4.2</p>
        </div>

        <h3 class="mt-8">7.1 最值得读的不是分数，而是 ReAct 与 AgentSPEX 的差异</h3>
        <p>
            论文讨论里有一个有启发性的现象：ReAct baseline 和 AgentSPEX 使用同一份 workflow，但 ReAct 是“把 workflow 放进 prompt 让模型自由执行”，AgentSPEX 是“让 harness 强制按步骤执行”。
            在 ELAIPBench 中，ReAct 甚至低于 CoT（33.8% vs. 37.2%）；在 ChemBench 中也低于 CoT（77.8% vs. 78.9%）。
            作者的解释是：把结构化 workflow 塞进 prompt 反而增加了模型负担，模型既要理解工作流结构，又要完成任务推理；如果由解释器负责控制流，模型就不必在每一步重新解释流程。
        </p>
        <p>
            这个结论对工程实践很重要：我们不能简单地认为“把流程写进 system prompt 就等于有了流程”。
            对长任务来说，流程应该是运行时系统的责任，而不只是文本建议。模型可以违反建议，但解释器不会跳过未满足的分支条件；模型可能忘记变量，但上下文管理器可以保存命名状态；模型可能重复执行上游步骤，但 checkpoint/replay 可以避免无谓重跑。
        </p>

        <h3 class="mt-8">7.2 用户研究：可读性更强，但复杂性信心仍需建立</h3>
        <p>
            论文还进行了一个 23 人 Google Forms 用户研究。参与者看到两个实现相同行为的 workflow：一个用 AgentSPEX 写，一个用 LangGraph 写；然后评价可解释性、从零开始构建的便利性、prompt 可见性和复杂多步 workflow 的偏好。
            定性结果显示，参与者普遍认为 AgentSPEX 更易读、prompt 更清楚、从零创建更轻；但在构建复杂多步 Agent 时，很多人仍倾向 LangGraph，认为它更可定制、更严谨。
        </p>
        <p>
            这其实指出了声明式 DSL 的典型张力：它降低了入门和维护成本，但当用户需要非常复杂的逻辑、深度框架扩展或大量自定义数据结构时，通用编程语言仍然更让人有掌控感。
            因此 AgentSPEX 更适合被理解为“把常见 Agent workflow 工程化”的中间层，而不是替代所有 Python 编排代码。
        </p>
    </div>

    <div class="card" id="practice">
        <h2>8. Practice：真实系统里应该如何使用这种 DSL</h2>
        <p>
            如果把 AgentSPEX 当成工程模式，而不只是论文系统，它给我们的第一条建议是：先识别哪些部分应该由模型自由发挥，哪些部分应该由框架确定执行。
            例如“搜索哪些关键词”“如何总结论文贡献”“怎样解释一个失败测试”可以交给 LLM；但“必须先读取问题，再定位文件，再修改，再运行测试，再写报告”这类阶段顺序最好由工作流控制。
            把这两类责任混在一个长 prompt 里，短期写起来快，长期会变成难以调试的隐式协议。
        </p>
        <p>
            第二条建议是：把状态设计成 API，而不是把历史当数据库。一个好 workflow 不应该让后续步骤读取一整段杂乱 conversation，而应该显式命名中间结果，例如 <code>requirements_json</code>、<code>candidate_files</code>、<code>test_failures</code>、<code>patch_summary</code>、<code>review_decision</code>。
            这些变量越清楚，后续的提示词越短，审计和回放也越容易。
        </p>
        <p>
            第三条建议是：把 workflow 切成可复用子模块。一个 deep research Agent 可以拆成 query generation、parallel search、source extraction、claim verification、report writing；一个 coding Agent 可以拆成 issue understanding、repository localization、patch planning、edit execution、test repair、final explanation。
            子模块不只是为了复用，也是为了测试和替换。你可以单独提高 search module 的召回率，而不影响报告写作模块；也可以把一个 module 从 LLM 调用换成确定性脚本。
        </p>

        <div class="grid grid-cols-1 gap-6 md:grid-cols-2 mt-6">
            <div class="rounded-2xl border border-white/70 bg-white/80 p-5">
                <h3 class="text-lg font-semibold text-slate-800">适合 AgentSPEX 的场景</h3>
                <ul class="list-disc pl-6 text-sm text-gray-600 space-y-2 mt-3">
                    <li>多阶段研究、写作、评审、代码修复或数据处理流程。</li>
                    <li>需要让领域专家编辑 prompt 和流程，但不希望他们修改 Python 框架代码。</li>
                    <li>需要日志、检查点、恢复、回放和可视化审计的长任务。</li>
                    <li>任务有稳定骨架，但每个节点内部仍需要 LLM 处理开放问题。</li>
                </ul>
            </div>
            <div class="rounded-2xl border border-white/70 bg-white/80 p-5">
                <h3 class="text-lg font-semibold text-slate-800">不应该过度使用的场景</h3>
                <ul class="list-disc pl-6 text-sm text-gray-600 space-y-2 mt-3">
                    <li>只有一次模型调用的简单问答，声明式 workflow 反而增加开销。</li>
                    <li>高度动态、每一步都要临时生成新程序结构的探索型任务。</li>
                    <li>需要复杂类型系统、事务语义或强一致性数据操作的后端服务。</li>
                    <li>安全边界尚未设计清楚，却给 Agent 暴露大量高权限工具的场景。</li>
                </ul>
            </div>
        </div>
    </div>

    <div class="card" id="limits">
        <h2>9. Discussion：局限、误区与未来方向</h2>
        <p>
            AgentSPEX 的第一类局限来自 DSL 本身。声明式语言的可读性建立在“常见模式足够覆盖需求”的前提上；一旦 workflow 作者需要复杂数据结构、动态生成节点、跨模块共享复杂对象，YAML 可能会变得冗长。
            这不是 AgentSPEX 独有的问题，而是所有配置化系统都会遇到的边界：当声明式配置开始模拟通用编程语言时，它可能同时失去简洁性和表达力。
        </p>
        <p>
            第二类局限来自模型非确定性。即使 control flow 被 harness 强制执行，节点内部的 LLM 输出仍可能不稳定。AgentSPEX 可以通过显式状态、工具限制、检查点和回放降低混乱，但不能保证每个模型判断都正确。
            因此关键生产流程仍需要验证器、测试、schema 检查、人类审批或沙箱权限控制。把 workflow 写成 YAML 不是安全性的终点，而是让安全和验证有更清楚的插入点。
        </p>
        <p>
            第三类局限来自评测解释。论文在七个 benchmark 上报告了 AgentSPEX 领先结果，但许多 workflow 是人工在编码助手帮助下编写的，不同任务的 workflow 设计质量可能影响分数。
            因此这些结果更适合说明“强制执行的结构化 workflow 有潜力提升长任务表现”，而不应被解读为“任意 YAML 化都会带来收益”。
            真正的工程问题是：如何系统地设计、搜索、验证和迁移这些 workflow。
        </p>
        <p>
            论文结论中给出的未来方向也围绕这些问题展开：更完整的 formal verification、训练模型自动编写和使用 workflow、把端到端 agentic training pipeline 纳入框架、增强多 Agent orchestration，以及用更强的上下文压缩和抽象支持更长任务。
            从更宏观的角度看，AgentSPEX 反映了 Agent 系统正在从“prompt engineering”走向“workflow engineering”：prompt 仍然重要，但它需要被放进状态、工具、权限、日志、验证和恢复机制之中。
        </p>
    </div>

    <div class="card" id="figures">
        <h2>10. Original Figures &amp; References</h2>
        <p>
            这一节保留论文原图，方便快速回看。需要注意：论文的 Figure 2 是 YAML 代码 listing，已在第 3 节以代码块形式保留；Table 1、Table 2、Table 3 的核心信息也分别融入语言构造表、benchmark 表和框架比较讨论中。
        </p>
        <div class="grid grid-cols-1 gap-6 md:grid-cols-2 mt-6">
            <figure class="bg-white/70 border border-white/70 rounded-2xl p-4">
                <img src="/notes/agentspex/figure-1-system-architecture.png" alt="Figure 1: AgentSPEX architecture" class="w-full rounded-xl" loading="lazy" />
                <figcaption class="text-sm text-gray-500 mt-3">Figure 1. An overview of the AgentSPEX Architecture.</figcaption>
            </figure>
            <figure class="bg-white/70 border border-white/70 rounded-2xl p-4">
                <img src="/notes/agentspex/figure-3-visual-editor.png" alt="Figure 3: AgentSPEX visual editor" class="w-full rounded-xl" loading="lazy" />
                <figcaption class="text-sm text-gray-500 mt-3">Figure 3. Visual editor interface for a deep research agent implemented with AgentSPEX.</figcaption>
            </figure>
            <figure class="bg-white/70 border border-white/70 rounded-2xl p-4 md:col-span-2">
                <img src="/notes/agentspex/figure-4-dashboard.png" alt="Figure 4: AgentSPEX dashboard" class="w-full rounded-xl" loading="lazy" />
                <figcaption class="text-sm text-gray-500 mt-3">Figure 4. Example of the log viewer for a SWE-Bench Verified instance.</figcaption>
            </figure>
        </div>

        <h3 class="mt-8">参考资料</h3>
        <ul class="list-disc pl-6 text-gray-600 space-y-2">
            <li><a href="https://arxiv.org/html/2604.13346v1" class="text-[color:var(--accent)] underline">AgentSPEX: An Agent SPecification and EXecution Language — arXiv HTML</a>：本文最主要的一手来源，用于提取摘要、系统设计、语言构造、评测和用户研究。</li>
            <li><a href="https://arxiv.org/pdf/2604.13346v1" class="text-[color:var(--accent)] underline">AgentSPEX paper PDF</a>：用于核对 Table 2 评测数值、附录 benchmark 细节、dashboard、formal verification 与 user study 问卷。</li>
            <li><a href="https://github.com/ScaleML/AgentSPEX" class="text-[color:var(--accent)] underline">ScaleML/AgentSPEX GitHub repository</a>：用于补充安装、运行、CLI、persistent VM、resume、replay、项目目录和工具扩展方式。</li>
            <li><a href="https://github.com/ScaleML/AgentSPEX/blob/main/docs/workflow-language.md" class="text-[color:var(--accent)] underline">AgentSPEX Workflow Language Documentation</a>：用于核对 YAML 顶层字段、<code>task</code>/<code>step</code> 语义、控制流、并发、子模块、memory 与工具过滤规则。</li>
            <li><a href="https://agentspex.ai/" class="text-[color:var(--accent)] underline">AgentSPEX project website</a>：用于补充官网对可视化编辑器、benchmark 图、demo agent 和 dashboard 的公开说明。</li>
        </ul>
    </div>
</BaseLayout>
