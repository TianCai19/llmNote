---
import BaseLayout from '../../layouts/BaseLayout.astro';
import UniCLIPArchitectureDiagram from '../../components/UniCLIPArchitectureDiagram.astro';
import { todayYmd } from '../../utils/date';

const createdAt = todayYmd();

export const meta = {
    number: '20',
    title: 'UniCLIP：统一对比学习框架',
    description: 'NeurIPS 2022 —— 融合图文、图图、文文三路对比的统一预训练框架',
    tags: ['UniCLIP', 'Contrastive', 'Multimodal'],
    readTime: '18 min'
};
---

<BaseLayout title="UniCLIP：统一对比学习框架" description="NeurIPS 2022 —— 融合图文、图图、文文三路对比的统一预训练框架">
    <header class="text-center mb-12">
        <h1 class="text-4xl font-bold mb-4 gradient-text">UniCLIP：统一对比学习框架</h1>
        <p class="text-xl text-gray-500">NeurIPS 2022 —— 当图文对比遇上自监督，三路统一的视觉-语言预训练</p>
        <p class="mt-4 text-sm uppercase tracking-[0.2em] text-[color:var(--muted)]">添加时间 {createdAt}</p>
    </header>

    <!-- 前言 -->
    <div class="card">
        <h2>前言：CLIP 还能更好吗？</h2>
        <p>CLIP 通过图文对比学习取得了巨大成功，但它<strong>只用了一种对比信号</strong> —— 图像与文本之间的匹配关系。自监督学习领域（SimCLR、MoCo、BYOL）早已证明，<strong>同模态内部的对比</strong>也能学到非常强的表示。</p>
        <p>UniCLIP 的核心问题是：<span class="highlight">能不能把跨模态对比（CLIP）和单模态自监督对比（SimCLR 风格）统一到一个框架里，让它们互相增强？</span></p>
        <div class="bg-blue-50 p-4 rounded-lg border border-blue-100">
            <strong>论文：</strong> <em>"UniCLIP: Unified Framework for Contrastive Language–Image Pre-training"</em>，NeurIPS 2022。核心贡献：提出三路统一对比学习框架（ITC + IIC + TTC），在零样本和线性探测任务上超越 CLIP。
        </div>
    </div>

    <!-- 第一章：三路对比 -->
    <div class="card">
        <h2>1. 三路对比学习目标</h2>
        <p>UniCLIP 将三种对比信号统一到同一个训练框架中：</p>

        <UniCLIPArchitectureDiagram />

        <h3 class="text-blue-600">ITC：Image-Text Contrastive（图文对比）</h3>
        <p>和 CLIP 完全一致：一个 batch 中的 N 个图文对，最大化匹配对的余弦相似度，最小化不匹配对。</p>
        <div class="math-block">
            <p set:html={"$$\\mathcal{L}_{ITC} = -\\frac{1}{2N}\\sum_{i=1}^{N}\\left(\\log\\frac{\\exp(\\text{sim}(I_i, T_i)/\\tau)}{\\sum_j \\exp(\\text{sim}(I_i, T_j)/\\tau)} + \\log\\frac{\\exp(\\text{sim}(I_i, T_i)/\\tau)}{\\sum_j \\exp(\\text{sim}(I_j, T_i)/\\tau)}\\right)$$"}></p>
        </div>

        <h3 class="text-blue-600">IIC：Image-Image Contrastive（图图对比）</h3>
        <p>灵感来自 SimCLR：对同一张图像进行两种不同的数据增强（裁剪、翻转、颜色抖动等），生成两个"视角"。这两个视角的嵌入应该相近，而不同图像的视角应该远离。</p>
        <div class="math-block">
            <p set:html={"$$\\mathcal{L}_{IIC} = -\\frac{1}{N}\\sum_{i=1}^{N}\\log\\frac{\\exp(\\text{sim}(I_i, I_i')/\\tau)}{\\sum_j \\exp(\\text{sim}(I_i, I_j')/\\tau)}$$"}></p>
        </div>
        <p>其中 <span set:html={"$I_i'$"}></span> 是图像 <span set:html={"$I_i$"}></span> 的增强版本。</p>

        <h3 class="text-blue-600">TTC：Text-Text Contrastive（文文对比）</h3>
        <p>文本的"增强"更加巧妙 —— 利用 <strong>Dropout 噪声</strong>。同一段文本经过编码器两次（每次 Dropout 随机丢弃不同的神经元），得到两个略有不同的嵌入。这种方式在 SimCSE 中已被验证有效。</p>
        <div class="math-block">
            <p set:html={"$$\\mathcal{L}_{TTC} = -\\frac{1}{N}\\sum_{i=1}^{N}\\log\\frac{\\exp(\\text{sim}(T_i, T_i')/\\tau)}{\\sum_j \\exp(\\text{sim}(T_i, T_j')/\\tau)}$$"}></p>
        </div>

        <div class="note-box">
            <strong>为什么三路比一路好？</strong> ITC 学习跨模态对齐，但不关心同模态内部的结构。IIC 让视觉编码器对增强不变，提升鲁棒性。TTC 让文本编码器对 Dropout 噪声不变，得到更紧凑的文本表示。三者互补，共同提升表征质量。
        </div>
    </div>

    <!-- 第二章：统一损失 -->
    <div class="card">
        <h2>2. 统一损失函数</h2>
        <p>UniCLIP 的总损失是三项的加权和：</p>
        <div class="math-block">
            <p set:html={"$$\\mathcal{L} = \\mathcal{L}_{ITC} + \\alpha \\cdot \\mathcal{L}_{IIC} + \\beta \\cdot \\mathcal{L}_{TTC}$$"}></p>
        </div>
        <p>其中 <span set:html={"$\\alpha$"}></span> 和 <span set:html={"$\\beta$"}></span> 是超参数，控制各路对比的权重。论文实验表明 <span set:html={"$\\alpha = \\beta = 1.0$"}></span> 在大多数情况下就足够好。</p>

        <h3 class="text-blue-600">训练策略</h3>
        <div class="overflow-x-auto">
            <table class="min-w-full divide-y divide-gray-200">
                <thead class="bg-gray-50">
                    <tr>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">参数</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">值</th>
                    </tr>
                </thead>
                <tbody class="bg-white divide-y divide-gray-200 text-sm">
                    <tr>
                        <td class="px-6 py-4 font-medium text-gray-700">图像增强</td>
                        <td class="px-6 py-4 text-gray-600">随机裁剪 + 水平翻转 + 颜色抖动 + 高斯模糊（SimCLR 风格）</td>
                    </tr>
                    <tr>
                        <td class="px-6 py-4 font-medium text-gray-700">文本增强</td>
                        <td class="px-6 py-4 text-gray-600">Dropout 噪声（两次前向传播，不同 Dropout mask）</td>
                    </tr>
                    <tr>
                        <td class="px-6 py-4 font-medium text-gray-700">温度参数</td>
                        <td class="px-6 py-4 text-gray-600">可学习温度，初始值 0.07（同 CLIP）</td>
                    </tr>
                    <tr>
                        <td class="px-6 py-4 font-medium text-gray-700">Batch Size</td>
                        <td class="px-6 py-4 text-gray-600">大 batch 对三路对比都很关键（提供更多负样本）</td>
                    </tr>
                    <tr>
                        <td class="px-6 py-4 font-medium text-gray-700">权重系数</td>
                        <td class="px-6 py-4 text-gray-600">α = β = 1.0（默认等权）</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>

    <!-- 第三章：架构设计 -->
    <div class="card">
        <h2>3. 架构设计要点</h2>

        <h3 class="text-blue-600">编码器选择</h3>
        <p>UniCLIP 保留了 CLIP 的双编码器架构，但有两个关键增强：</p>
        <ul class="list-disc pl-6 mb-4 space-y-2 text-gray-600">
            <li><strong>Projection Head</strong>：编码器输出后接一个 MLP 投影头（2-3 层），对比损失在投影空间计算。这是 SimCLR 的关键发现 —— 投影头让编码器保留更多信息</li>
            <li><strong>共享投影空间</strong>：三路对比都在同一个 <span set:html={"$d$"}></span> 维空间中进行，确保表示的一致性</li>
        </ul>

        <h3 class="text-blue-600">数据增强策略</h3>
        <p>图像和文本使用不同的增强方式：</p>
        <div class="overflow-x-auto">
            <table class="min-w-full divide-y divide-gray-200">
                <thead class="bg-gray-50">
                    <tr>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">模态</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">增强方式</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">灵感来源</th>
                    </tr>
                </thead>
                <tbody class="bg-white divide-y divide-gray-200 text-sm">
                    <tr>
                        <td class="px-6 py-4 font-medium text-blue-600">图像</td>
                        <td class="px-6 py-4 text-gray-600">随机裁剪 + 翻转 + 颜色抖动 + 模糊</td>
                        <td class="px-6 py-4 text-gray-600">SimCLR / BYOL</td>
                    </tr>
                    <tr>
                        <td class="px-6 py-4 font-medium text-blue-600">文本</td>
                        <td class="px-6 py-4 text-gray-600">Dropout mask 差异（同一输入两次前向）</td>
                        <td class="px-6 py-4 text-gray-600">SimCSE</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>

    <!-- 第四章：实验结果 -->
    <div class="card">
        <h2>4. 实验结果</h2>
        <p>UniCLIP 在多个基准上超越了同等规模的 CLIP：</p>

        <h3 class="text-blue-600">零样本分类</h3>
        <div class="overflow-x-auto">
            <table class="min-w-full divide-y divide-gray-200">
                <thead class="bg-gray-50">
                    <tr>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">模型</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">ImageNet Top-1</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">说明</th>
                    </tr>
                </thead>
                <tbody class="bg-white divide-y divide-gray-200 text-sm">
                    <tr>
                        <td class="px-6 py-4 text-gray-600">CLIP (ViT-B/16)</td>
                        <td class="px-6 py-4 text-gray-600">68.3%</td>
                        <td class="px-6 py-4 text-gray-600">基线</td>
                    </tr>
                    <tr>
                        <td class="px-6 py-4 font-medium text-blue-600">UniCLIP (ViT-B/16)</td>
                        <td class="px-6 py-4 font-medium text-blue-600">70.2%</td>
                        <td class="px-6 py-4 text-gray-600">+1.9%，三路对比的增益</td>
                    </tr>
                </tbody>
            </table>
        </div>

        <h3 class="text-blue-600">线性探测 (Linear Probe)</h3>
        <p>冻结编码器，只训练线性分类头：</p>
        <ul class="list-disc pl-6 mb-4 space-y-2 text-gray-600">
            <li>ImageNet linear probe 提升约 <strong>1-2%</strong></li>
            <li>在多个细粒度数据集（Stanford Cars, FGVC Aircraft）上提升更明显</li>
            <li>IIC 的贡献在图像相关任务上尤为突出</li>
        </ul>

        <h3 class="text-blue-600">消融实验</h3>
        <div class="overflow-x-auto">
            <table class="min-w-full divide-y divide-gray-200">
                <thead class="bg-gray-50">
                    <tr>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">配置</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">ITC</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">IIC</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">TTC</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">ImageNet ZS</th>
                    </tr>
                </thead>
                <tbody class="bg-white divide-y divide-gray-200 text-sm">
                    <tr>
                        <td class="px-6 py-4 text-gray-600">CLIP 基线</td>
                        <td class="px-6 py-4 text-center">✓</td>
                        <td class="px-6 py-4 text-center">✗</td>
                        <td class="px-6 py-4 text-center">✗</td>
                        <td class="px-6 py-4 text-gray-600">68.3%</td>
                    </tr>
                    <tr>
                        <td class="px-6 py-4 text-gray-600">+ IIC</td>
                        <td class="px-6 py-4 text-center">✓</td>
                        <td class="px-6 py-4 text-center">✓</td>
                        <td class="px-6 py-4 text-center">✗</td>
                        <td class="px-6 py-4 text-gray-600">69.5%</td>
                    </tr>
                    <tr>
                        <td class="px-6 py-4 text-gray-600">+ TTC</td>
                        <td class="px-6 py-4 text-center">✓</td>
                        <td class="px-6 py-4 text-center">✗</td>
                        <td class="px-6 py-4 text-center">✓</td>
                        <td class="px-6 py-4 text-gray-600">69.1%</td>
                    </tr>
                    <tr>
                        <td class="px-6 py-4 font-medium text-blue-600">UniCLIP（全部）</td>
                        <td class="px-6 py-4 text-center">✓</td>
                        <td class="px-6 py-4 text-center">✓</td>
                        <td class="px-6 py-4 text-center">✓</td>
                        <td class="px-6 py-4 font-medium text-blue-600">70.2%</td>
                    </tr>
                </tbody>
            </table>
        </div>
        <p>消融实验清楚地表明：IIC 和 TTC 各自都有正向贡献，三者组合效果最佳。</p>
    </div>

    <!-- 第五章：与 CLIP 对比 -->
    <div class="card">
        <h2>5. UniCLIP vs CLIP：核心差异</h2>
        <div class="overflow-x-auto">
            <table class="min-w-full divide-y divide-gray-200">
                <thead class="bg-gray-50">
                    <tr>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">维度</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">CLIP</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">UniCLIP</th>
                    </tr>
                </thead>
                <tbody class="bg-white divide-y divide-gray-200 text-sm">
                    <tr>
                        <td class="px-6 py-4 font-medium text-gray-700">对比信号</td>
                        <td class="px-6 py-4 text-gray-600">仅图文对比（ITC）</td>
                        <td class="px-6 py-4 text-gray-600">图文 + 图图 + 文文（ITC + IIC + TTC）</td>
                    </tr>
                    <tr>
                        <td class="px-6 py-4 font-medium text-gray-700">数据增强</td>
                        <td class="px-6 py-4 text-gray-600">简单的随机裁剪</td>
                        <td class="px-6 py-4 text-gray-600">SimCLR 风格强增强 + Dropout 增强</td>
                    </tr>
                    <tr>
                        <td class="px-6 py-4 font-medium text-gray-700">Projection Head</td>
                        <td class="px-6 py-4 text-gray-600">简单线性层</td>
                        <td class="px-6 py-4 text-gray-600">MLP 投影头（2-3 层）</td>
                    </tr>
                    <tr>
                        <td class="px-6 py-4 font-medium text-gray-700">单模态表示</td>
                        <td class="px-6 py-4 text-gray-600">仅通过跨模态间接学习</td>
                        <td class="px-6 py-4 text-gray-600">显式优化同模态不变性</td>
                    </tr>
                    <tr>
                        <td class="px-6 py-4 font-medium text-gray-700">训练成本</td>
                        <td class="px-6 py-4 text-gray-600">基线</td>
                        <td class="px-6 py-4 text-gray-600">略高（额外增强 + 前向传播）</td>
                    </tr>
                    <tr>
                        <td class="px-6 py-4 font-medium text-gray-700">零样本性能</td>
                        <td class="px-6 py-4 text-gray-600">强</td>
                        <td class="px-6 py-4 text-gray-600">更强（+1-2%）</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>

    <!-- 第六章：伪代码 -->
    <div class="card">
        <h2>6. PyTorch 伪代码</h2>
        <pre class="language-python"><code>{`# UniCLIP 统一对比损失（伪代码）
import torch
import torch.nn.functional as F

# 图像：原图 + 增强图
I = image_encoder(images)          # [N, d]
I_aug = image_encoder(aug(images)) # [N, d]

# 文本：两次前向（不同 dropout mask）
T = text_encoder(texts)            # [N, d]
T_aug = text_encoder(texts)        # [N, d]  不同 dropout

# L2 归一化
I, I_aug = F.normalize(I, dim=-1), F.normalize(I_aug, dim=-1)
T, T_aug = F.normalize(T, dim=-1), F.normalize(T_aug, dim=-1)

# ITC: 图文对比 (同 CLIP)
logits_it = (I @ T.T) * tau.exp()
labels = torch.arange(N)
L_itc = (F.cross_entropy(logits_it, labels)
       + F.cross_entropy(logits_it.T, labels)) / 2

# IIC: 图图对比
logits_ii = (I @ I_aug.T) * tau.exp()
L_iic = F.cross_entropy(logits_ii, labels)

# TTC: 文文对比
logits_tt = (T @ T_aug.T) * tau.exp()
L_ttc = F.cross_entropy(logits_tt, labels)

# 统一损失
loss = L_itc + alpha * L_iic + beta * L_ttc`}</code></pre>
    </div>

    <!-- 总结 -->
    <div class="card">
        <h2>总结</h2>
        <div class="overflow-x-auto">
            <table class="min-w-full divide-y divide-gray-200">
                <thead class="bg-gray-50">
                    <tr>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">维度</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">要点</th>
                    </tr>
                </thead>
                <tbody class="bg-white divide-y divide-gray-200 text-sm">
                    <tr>
                        <td class="px-6 py-4 font-medium text-blue-600">核心创新</td>
                        <td class="px-6 py-4 text-gray-600">三路统一对比：ITC + IIC + TTC</td>
                    </tr>
                    <tr>
                        <td class="px-6 py-4 font-medium text-blue-600">图像增强</td>
                        <td class="px-6 py-4 text-gray-600">SimCLR 风格强增强（裁剪、翻转、颜色、模糊）</td>
                    </tr>
                    <tr>
                        <td class="px-6 py-4 font-medium text-blue-600">文本增强</td>
                        <td class="px-6 py-4 text-gray-600">Dropout 噪声（SimCSE 思路）</td>
                    </tr>
                    <tr>
                        <td class="px-6 py-4 font-medium text-blue-600">性能提升</td>
                        <td class="px-6 py-4 text-gray-600">零样本 ImageNet +1.9%，线性探测 +1-2%</td>
                    </tr>
                    <tr>
                        <td class="px-6 py-4 font-medium text-blue-600">启示</td>
                        <td class="px-6 py-4 text-gray-600">跨模态和同模态对比可以互补，统一框架 > 单一目标</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>

    <div class="mt-8 text-center text-gray-500">
        <p>UniCLIP 告诉我们：对比学习的信号越丰富，表示就越通用 —— 不要只看模态之间，也要看模态内部。</p>
    </div>
</BaseLayout>
